openapi: 3.1.0
info:
  title: Pimienta Alimentos API
  description: "REST API for Pimienta Alimentos. Most endpoints require a Bearer JWT\
    \ access token. Role claims are exposed as Spring authorities `ROLE_<NAME>` (e.g.\
    \ `ROLE_ADMIN`, `ROLE_MANAGER`)."
  contact:
    name: Pimienta Alimentos
    url: https://github.com/alexistrejo11/pimienta
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
  version: v1
servers:
- url: http://localhost:8080
  description: Generated server url
tags:
- name: Inventory — storage locations
  description: "Ubicaciones de almacén (árbol, bloqueo). Requiere Bearer JWT. Errores:\
    \ `STORAGE_LOCATION_NOT_FOUND`, `STORAGE_LOCATION_NOT_EMPTY`."
- name: Notification logs
  description: Manager-facing read-only log of today's LOG-channel notifications.
    Requires JWT with **ROLE_MANAGER** or **ROLE_ADMIN**.
- name: Account — Profile
  description: "Current-user profile and dashboard. Requires `Authorization: Bearer\
    \ <access_token>`. JWT must map to a valid user; roles are exposed as `ROLE_*`\
    \ authorities."
- name: HR — Employees
  description: "Employee lifecycle: search, registration, updates, contract submission,\
    \ termination/rehire, position changes, Excel import/export, and statistics. Requires\
    \ `Authorization: Bearer <access_token>`. Typical roles: **MANAGER**, **ADMIN**,\
    \ **HR** (as configured). Not-found responses use `EMPLOYEE_NOT_FOUND` in `ApiErrorResponse`."
- name: Authentication
  description: Registration and session endpoints. **Register** and **login** are
    public (no Bearer token). **Refresh** and **logout** are also reachable without
    an `Authorization` header in this configuration; they use the refresh token carried
    in the JSON body where applicable.
- name: Operations — Headquarters
  description: "Company sites / branches: CRUD, lookup by id or name, statistics,\
    \ and Excel import/export. Requires `Authorization: Bearer <access_token>`. Typical\
    \ roles: **ADMIN**, **MANAGER**, **OPERATIONS** (as configured). Not-found responses\
    \ use `HEADQUARTER_NOT_FOUND` in `ApiErrorResponse`."
- name: Inventory — movements
  description: "Historial de movimientos de stock (consulta). Requiere Bearer JWT.\
    \ **404**: `INVENTORY_MOVEMENT_NOT_FOUND`. Rate limit clase: **STANDARD**; lecturas:\
    \ **READ_HEAVY**."
- name: Account — User management
  description: "Administrative APIs under `/api/v1/users/management`. **Requires**\
    \ `Authorization: Bearer <access_token>`. Document **elevated** roles (`ROLE_ADMIN`,\
    \ `ROLE_MANAGER`, `ROLE_SUPPORT`) for operators; fine-grained authorization may\
    \ be enforced in the application layer as the product evolves."
- name: Notification management
  description: Administrator APIs for searching stored notifications and viewing delivery
    statistics. Requires JWT with **ROLE_ADMIN**.
- name: CRM — Opportunities
  description: "Sales opportunities: search, CRUD, Excel import/export, pipeline transitions\
    \ (discovery → win/lose), and nested tasks. Requires `Authorization: Bearer <access_token>`.\
    \ Typical roles: **MANAGER**, **ADMIN**, **SUPPORT** (as configured)."
- name: Payroll
  description: Payroll management and reporting endpoints.
- name: HR — Employee work schedule
  description: "Weekly work schedule stored on the employee record (JSON). **GET**\
    \ returns the configured slots (possibly empty). **PUT** replaces the entire schedule.\
    \ Requires `Authorization: Bearer <access_token>` unless this environment still\
    \ permits the employees route for testing."
- name: Inventory — stock
  description: "Existencias por artículo y ubicación. Requiere Bearer JWT. Errores\
    \ típicos: `INVENTORY_NOT_FOUND`, `INVENTORY_ALREADY_EXISTS`, `ITEM_NOT_FOUND`,\
    \ `STORAGE_LOCATION_NOT_FOUND`."
- name: Contracts
  description: "Business agreements (employment, supplier, customer, …). Requires\
    \ `Authorization: Bearer <access_token>`. Typical operators: **ADMIN**, **MANAGER**.\
    \ Related entities (employee, opportunity, project) are validated when IDs are\
    \ supplied. Domain rules may return `CONTRACT_NOT_FOUND` or validation errors\
    \ (`errorCode` in `ApiErrorResponse`)."
- name: Inventory — items
  description: "Catálogo de artículos (SKU, categoría, estado). Requiere `Authorization:\
    \ Bearer <access_token>`. Operadores típicos: **ADMIN**, **MANAGER**. Errores\
    \ de dominio devuelven `ApiErrorResponse` (p. ej. `ITEM_NOT_FOUND`, `ITEM_SKU_ALREADY_EXISTS`)."
- name: File management
  description: "Administrator API for uploading, searching, deleting, and downloading\
    \ company files stored in S3 (pimienta/sources/). Requires JWT with **ROLE_ADMIN**."
- name: Inventory — transactions
  description: "Movimientos de inventario (compra, venta, transferencia, ajustes,\
    \ flujo de aprobación). Requiere Bearer JWT. Errores: `INVENTORY_TRANSACTION_NOT_FOUND`,\
    \ `INVENTORY_TRANSACTION_INVALID_STATE`, validaciones de negocio."
- name: CRM — Project milestones
  description: "Milestones under a project: CRUD and lifecycle (start, complete, delayed,\
    \ cancel, billed). Requires `Authorization: Bearer <access_token>`. **404** when\
    \ project or milestone is unknown (`PROJECT_NOT_FOUND`, `PROJECT_MILESTONE_NOT_FOUND`)."
- name: Operations — Tasks
  description: "Work items: search, CRUD, status and assignment updates, checklist\
    \ toggles, and Excel import/export. Requires `Authorization: Bearer <access_token>`.\
    \ Typical roles: **MANAGER**, **ADMIN**, **SUPPORT** (as configured). Not-found\
    \ responses may use `TASK_NOT_FOUND` or `TASK_CHECKLIST_ITEM_NOT_FOUND` in `ApiErrorResponse`."
- name: File resources
  description: Module-scoped file upload and retrieval (RESOURCE category). Requires
    JWT with **ROLE_ADMIN** or **ROLE_MANAGER**.
- name: Health
  description: "Public liveness checks. No authentication required; intended for load\
    \ balancers, container orchestrators, and uptime monitors."
- name: HR — Employee attendance
  description: "Check-in / check-out for employees, queries by id, by headquarter\
    \ for the current day, paginated search, and automatic closure of forgotten open\
    \ attendances (scheduled job). Photo fields accept caller-managed URLs. Requires\
    \ `Authorization: Bearer <access_token>` unless this environment still permits\
    \ the employees route for testing."
- name: CRM — Projects
  description: "Delivery projects: search, CRUD, lifecycle (activate, hold, complete,\
    \ cancel, archive), and nested tasks. Requires `Authorization: Bearer <access_token>`."
paths:
  /api/v1/inventory/locations/{id}:
    get:
      tags:
      - Inventory — storage locations
      summary: Get storage location by id
      description: "**404** (`STORAGE_LOCATION_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getLocationById
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 2
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - Inventory — storage locations
      summary: Update storage location
      description: "JSON **StorageLocationUpdateRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: updateLocation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 2
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StorageLocationUpdateRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - Inventory — storage locations
      summary: Delete storage location
      description: "**204** si ok. Puede fallar si no está vacía (`STORAGE_LOCATION_NOT_EMPTY`).\
        \ Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: deleteLocation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 2
      responses:
        "204":
          description: Eliminada.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/locations/{id}/unblock:
    put:
      tags:
      - Inventory — storage locations
      summary: Unblock storage location
      description: "Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: unblockLocation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 2
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/locations/{id}/block:
    put:
      tags:
      - Inventory — storage locations
      summary: Block storage location
      description: "Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: blockLocation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 2
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/items/{id}:
    get:
      tags:
      - Inventory — items
      summary: Get item by id
      description: "**404** si no existe (`ITEM_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getItemById
      parameters:
      - name: id
        in: path
        description: Identificador del artículo.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: Artículo.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - Inventory — items
      summary: Update inventory item
      description: "Actualiza campos del artículo. **404** si no existe. Rate limit:\
        \ **SENSITIVE_OPERATIONS**."
      operationId: updateItem
      parameters:
      - name: id
        in: path
        description: Identificador del artículo.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      requestBody:
        description: Cuerpo JSON (**ItemUpdateRequest**).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemUpdateRequest"
        required: true
      responses:
        "200":
          description: Artículo actualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - Inventory — items
      summary: Delete inventory item
      description: "Elimina el artículo. **204** si ok. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: deleteItem
      parameters:
      - name: id
        in: path
        description: Identificador del artículo a eliminar.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "204":
          description: Sin contenido.
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/items/{id}/discontinue:
    put:
      tags:
      - Inventory — items
      summary: Discontinue item
      description: "Marca el artículo como descontinuado. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: discontinueItem
      parameters:
      - name: id
        in: path
        description: Identificador del artículo.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: Artículo actualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/items/{id}/activate:
    put:
      tags:
      - Inventory — items
      summary: Activate item
      description: "Reactiva un artículo descontinuado. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: activateItem
      parameters:
      - name: id
        in: path
        description: Identificador del artículo.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: Artículo actualizado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters/{id}:
    get:
      tags:
      - Operations — Headquarters
      summary: Get headquarter by id
      description: '**404** if not found (`HEADQUARTER_NOT_FOUND`).'
      operationId: getHeadquarterById
      parameters:
      - name: id
        in: path
        description: Headquarter id.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: Headquarter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeadQuarterResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - Operations — Headquarters
      summary: Update headquarter
      description: Replaces headquarter fields from **HeadQuarterRequest**. **404**
        if not found (`HEADQUARTER_NOT_FOUND`).
      operationId: updateHeadquarter
      parameters:
      - name: id
        in: path
        description: Headquarter id to update.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      requestBody:
        description: Same JSON schema as create.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeadQuarterRequest"
            examples:
              update:
                summary: Update
                description: update
                value:
                  name: North Plant — Monterrey (expansion)
                  address: "1200 Industrial Ave, Building 4"
                  description: Includes new packaging line.
        required: true
      responses:
        "200":
          description: Updated headquarter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeadQuarterResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - Operations — Headquarters
      summary: Soft-delete headquarter
      description: "Soft-deletes the site. **204** on success. Rate limit: **SENSITIVE_OPERATIONS**.\
        \ **404** if not found."
      operationId: softDeleteHeadquarter
      parameters:
      - name: id
        in: path
        description: Headquarter id to soft-delete.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "204":
          description: No content; headquarter soft-deleted.
        "404":
          description: Not found (`HEADQUARTER_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{id}:
    get:
      tags:
      - HR — Employees
      summary: Get employee by id
      description: '**404** if not found (`EMPLOYEE_NOT_FOUND`).'
      operationId: getEmployeeDetailById
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Employee detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - HR — Employees
      summary: Update employee
      description: |-
        Recommended: **`multipart/form-data`** parts:

        + **`employee`**: JSON matching **`UpdateEmployeeRequest`** (`Content-Type`:
          **`application/json`** when possible).
        + **`photo`** (optional): image (`image/jpeg`, `image/png`, …).

        Browsers/Swagger may label **`employee`** as **`application/octet-stream`**; the server parses
        the bytes as UTF-8 JSON anyway.

        For JSON-without-photo only, **`application/json`** is also accepted (alternate operation hidden
        in Swagger). **404** if not found (`EMPLOYEE_NOT_FOUND`). Rate limit: **SENSITIVE_OPERATIONS**.
      operationId: updateEmployeeMultipart
      parameters:
      - name: id
        in: path
        description: Identificador del empleado.
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      requestBody:
        description: "Must be **multipart/form-data** with parts **employee** (JSON,\
          \ UpdateEmployeeRequest) and optional **photo** (file)."
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/EmployeeUpdateMultipartBody"
              description: "Two form parts: **employee** (object) and optional **photo**\
                \ (binary)."
              required:
              - employee
            encoding:
              employee:
                contentType: application/json
              photo: {}
        required: true
      responses:
        "200":
          description: Updated employee.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - HR — Employees
      summary: Soft-delete employee
      description: "Soft-deletes the employee record. **204** on success. Rate limit:\
        \ **SENSITIVE_OPERATIONS**. **404** if not found."
      operationId: deleteEmployee
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No content; employee soft-deleted.
        "404":
          description: Not found (`EMPLOYEE_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{id}/terminate:
    put:
      tags:
      - HR — Employees
      summary: Terminate employee
      description: "Ends the employment when rules allow. Rate limit: **SENSITIVE_OPERATIONS**.\
        \ **404** if not found."
      operationId: terminateEmployee
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Terminated employee state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{id}/submit-for-contract:
    put:
      tags:
      - HR — Employees
      summary: Submit employee for contract
      description: Advances onboarding toward contract when business rules allow.
        **404** if not found (`EMPLOYEE_NOT_FOUND`); domain rules may yield **409**
        or validation errors.
      operationId: submitEmployeeForContract
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated employee.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{id}/rehire:
    put:
      tags:
      - HR — Employees
      summary: Rehire employee
      description: Restores active employment when rules allow. **404** if not found.
      operationId: rehireEmployee
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated employee.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{employeeId}/work-schedule:
    get:
      tags:
      - HR — Employee work schedule
      summary: Get employee work schedule
      description: '**404** if the employee does not exist (`EMPLOYEE_NOT_FOUND`).'
      operationId: getEmployeeWorkSchedule
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Current schedule (slots may be empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeWorkScheduleResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - HR — Employee work schedule
      summary: Replace employee work schedule
      description: Replaces the full weekly schedule. Each slot must have **startTime**
        strictly before **endTime** (HTTP **400** otherwise). **404** if the employee
        does not exist.
      operationId: replaceEmployeeWorkSchedule
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceEmployeeWorkScheduleRequest"
        required: true
      responses:
        "200":
          description: Updated schedule.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeWorkScheduleResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/contracts/{id}:
    get:
      tags:
      - Contracts
      summary: Get contract by id
      description: Loads one contract by id. **404** when missing or soft-deleted.
      operationId: getContract
      parameters:
      - name: id
        in: path
        description: Identificador del contrato.
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      responses:
        "200":
          description: Contract found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContractResponse"
        "404":
          description: Contract not found (`CONTRACT_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    put:
      tags:
      - Contracts
      summary: Replace contract
      description: Full update of contract fields (same rules as create for term dates
        and relationships). **404** if the contract does not exist.
      operationId: updateContract
      parameters:
      - name: id
        in: path
        description: Identificador del contrato a reemplazar.
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      requestBody:
        description: Estado completo del contrato (**UpdateContractRequest**).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateContractRequest"
            examples:
              updateSupplier:
                summary: Actualización proveedor
                description: updateSupplier
                value:
                  name: Contrato suministro 2025 (enmienda 1)
                  description: Incluye anexo de logística inversa.
                  category: SUPPLIER
                  employeeId: null
                  opportunityId: 5001
                  projectId: 88
                  termKind: FIXED_TERM
                  effectiveStart: 2025-05-01
                  effectiveEnd: 2026-10-31
                  documentUrl: https://storage.example.com/contracts/sup-2025-001-v2.pdf
                  termsAndConditions: Pago a 45 días.
                  referenceCode: CT-SUP-2025-042-R1
                  renewalCycleMonths: 12
                  agreedValue: 275000.0
                  currencyCode: MXN
        required: true
      responses:
        "200":
          description: Updated contract.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContractResponse"
        "404":
          description: Contract not found (`CONTRACT_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - Contracts
      summary: Soft-delete contract
      description: Marks the contract as deleted (`deletedAt`). **204** on success.
        **404** if not found.
      operationId: deleteContract
      parameters:
      - name: id
        in: path
        description: Identificador del contrato a dar de baja lógica.
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      responses:
        "204":
          description: No content; contract soft-deleted.
        "404":
          description: Contract not found (`CONTRACT_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/contracts/{id}/extend:
    put:
      tags:
      - Contracts
      summary: Extend contract end date
      description: Sets a new end date strictly after the current end (**FIXED_TERM**
        only). **400** when validation fails.
      operationId: extendContract
      parameters:
      - name: id
        in: path
        description: Contrato de plazo fijo cuya fecha de término se amplía manualmente.
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      requestBody:
        description: Nueva fecha de fin (**ExtendContractRequest**).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExtendContractRequest"
            examples:
              extendEnd:
                summary: Prorrogar al 2027
                description: extendEnd
                value:
                  newEnd: 2027-12-31
        required: true
      responses:
        "200":
          description: Contract after extension.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContractResponse"
        "404":
          description: Contract not found (`CONTRACT_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/{id}/unban:
    post:
      tags:
      - Account — User management
      summary: Unban user
      description: "Clears ban state. **204** on success. Rate limit: **SENSITIVE_OPERATIONS**.\
        \ Typically **ADMIN** / **MANAGER**."
      operationId: unbanUser
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: User unbanned; no response body.
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/{id}/roles:
    post:
      tags:
      - Account — User management
      summary: Add roles to user
      description: "Adds roles from the request body (enum names such as `MANAGER`,\
        \ `ADMIN`). Idempotent merge semantics are defined by the domain. Rate limit:\
        \ **SENSITIVE_OPERATIONS**. Typically **ADMIN**."
      operationId: addRoles
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddRolesRequest"
        required: true
      responses:
        "200":
          description: User with updated role set.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/{id}/ban:
    post:
      tags:
      - Account — User management
      summary: Ban user
      description: "Bans a user (optional reason in body). **204** on success. Rate\
        \ limit: **SENSITIVE_OPERATIONS**. Typically restricted to **ADMIN** / **MANAGER**\
        \ roles."
      operationId: banUser
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BanUserRequest"
      responses:
        "204":
          description: User banned; no response body.
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/{id}/approve:
    post:
      tags:
      - Account — User management
      summary: Approve pending user
      description: "Activates a previously pending account. **204** on success. Rate\
        \ limit: **SENSITIVE_OPERATIONS**. Typically **ADMIN** / **MANAGER**."
      operationId: approveUser
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: User approved; no response body.
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks:
    get:
      tags:
      - Operations — Tasks
      summary: Search tasks
      description: "Filtered, paginated list. Response shape is **PagedResponse**\
        \ (items + metadata). Rate limit: **READ_HEAVY**."
      operationId: searchTasks
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/TaskSearchRequest"
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size (1–100).
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      - name: headquarterId
        in: query
        description: Filter by headquarter id.
        schema:
          type: integer
          format: int64
        example: 1
      - name: projectId
        in: query
        description: Filter by project id.
        schema:
          type: integer
          format: int64
        example: 10
      - name: opportunityId
        in: query
        description: Filter by CRM opportunity id.
        schema:
          type: integer
          format: int64
        example: 500
      - name: employeeId
        in: query
        description: Filter by assignee (employee) id.
        schema:
          type: integer
          format: int64
        example: 7
      - name: status
        in: query
        description: Filter by workflow status.
        schema:
          type: string
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
          - DELAYED
          - ON_HOLD
          - FAILED
          - UNDEFINED
        example: PENDING
      responses:
        "200":
          description: Paged task rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Operations — Tasks
      summary: Create task
      description: Creates a task from **TaskRequest** (Jakarta format validation
        on the body).
      operationId: createTask
      requestBody:
        description: JSON body; **title** is required. **priority** defaults to MEDIUM
          when omitted.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskRequest"
            examples:
              minimal:
                summary: Title only
                description: minimal
                value:
                  title: Follow up with supplier
              full:
                summary: With checklist and scopes
                description: full
                value:
                  title: Prepare samples
                  description: Ship before Friday.
                  priority: HIGH
                  dueDate: 2026-04-20T18:00:00
                  headquarterId: 1
                  projectId: 10
                  opportunityId: 500
                  createdById: 3
                  checklist:
                  - description: Order packaging
                    displayOrder: 0
                  - description: Label boxes
                    displayOrder: 1
        required: true
      responses:
        "201":
          description: Created task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/import:
    post:
      tags:
      - Operations — Tasks
      summary: Import tasks from spreadsheet
      description: "Upload `multipart/form-data` with field **file** (`.xlsx`); bulk\
        \ upsert by ID column. Empty file yields **400**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: importTasksFromExcel
      requestBody:
        description: "Form part **file**: Excel workbook (.xlsx)."
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include part `file` with the Excel binary.
              required:
              - file
            encoding:
              file:
                contentType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        required: true
      responses:
        "200":
          description: Created/updated/skipped counts and row errors.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskBulkImportResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects:
    get:
      tags:
      - CRM — Projects
      summary: Search projects
      description: "Filtered, paginated list (`ProjectSearchRequest`). Rate limit:\
        \ **READ_HEAVY**."
      operationId: searchProjects
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/ProjectSearchRequest"
      responses:
        "200":
          description: Paged projects.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - CRM — Projects
      summary: Create project
      description: Creates a project from **CreateProjectRequest**. **409** if `projectCode`
        already exists (handled as conflict in the use case layer).
      operationId: createProject
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
        required: true
      responses:
        "201":
          description: Created project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "409":
          description: Duplicate project code.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones:
    get:
      tags:
      - CRM — Project milestones
      summary: List project milestones
      description: "Paged list for the given `projectId`. Rate limit: **READ_HEAVY**."
      operationId: listMilestones
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      responses:
        "200":
          description: Paged milestones.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - CRM — Project milestones
      summary: Create milestone
      description: Creates a milestone under the project.
      operationId: createMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectMilestoneRequest"
        required: true
      responses:
        "201":
          description: Created milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}/start:
    post:
      tags:
      - CRM — Project milestones
      summary: Start milestone
      description: "Lifecycle: start work when domain allows."
      operationId: startMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}/delayed:
    post:
      tags:
      - CRM — Project milestones
      summary: Mark milestone delayed
      description: "Lifecycle: delayed state."
      operationId: markDelayed
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}/complete:
    post:
      tags:
      - CRM — Project milestones
      summary: Complete milestone
      description: "Lifecycle: mark completed when allowed."
      operationId: completeMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}/cancel:
    post:
      tags:
      - CRM — Project milestones
      summary: Cancel milestone
      description: "Lifecycle: cancel. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: cancelMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}/billed:
    post:
      tags:
      - CRM — Project milestones
      summary: Mark milestone billed
      description: "Lifecycle: billed / invoiced state."
      operationId: markBilled
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/hold:
    post:
      tags:
      - CRM — Projects
      summary: Put project on hold
      description: Requires `ReasonRequest` body with reason text.
      operationId: putOnHold
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReasonRequest"
        required: true
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/complete:
    post:
      tags:
      - CRM — Projects
      summary: Complete project
      description: Marks project completed when domain allows.
      operationId: complete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/cancel:
    post:
      tags:
      - CRM — Projects
      summary: Cancel project
      description: "Cancellation with reason. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: cancel
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReasonRequest"
        required: true
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/archive:
    post:
      tags:
      - CRM — Projects
      summary: Archive project
      description: "Archive lifecycle. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: archive
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/activate:
    post:
      tags:
      - CRM — Projects
      summary: Activate project
      description: "Lifecycle: activate when allowed by domain."
      operationId: activate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/records:
    get:
      tags:
      - Payroll
      summary: List payroll records
      description: Returns filtered payroll records with pagination.
      operationId: listPayrollRecords
      parameters:
      - name: request
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PayrollRecordSearchRequest"
      - name: employeeId
        in: query
        description: Filter by employee id.
        example: 101
      - name: periodId
        in: query
        description: Filter by payroll period id.
        example: 22
      - name: page
        in: query
        description: Page index.
        example: 0
      - name: size
        in: query
        description: Page size.
        example: 20
      responses:
        "200":
          description: Paged payroll records.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Payroll
      summary: Create payroll record
      description: Creates payroll record for an employee and worked range.
      operationId: registerPayrollRecord
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPayrollRecordRequest"
            example:
              employeeId: 101
              periodId: 22
              workedDaysStart: 2026-04-01
              workedDaysEnd: 2026-04-07
              grossAmount: 3500.0
        required: true
      responses:
        "201":
          description: Record created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRecordResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/records/{recordId}/adjustments:
    post:
      tags:
      - Payroll
      summary: Register payroll adjustment
      description: Applies bonus/discount to a payroll record.
      operationId: registerAdjustment
      parameters:
      - name: recordId
        in: path
        description: Payroll record id.
        required: true
        schema:
          type: integer
          format: int64
        example: 3001
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPayrollAdjustmentRequest"
            example:
              type: DISCOUNT
              amount: 150.0
              reason: Late arrivals
        required: true
      responses:
        "201":
          description: Adjustment applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPaymentResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/periods:
    get:
      tags:
      - Payroll
      summary: List payroll periods
      description: Returns payroll periods with pagination.
      operationId: listPayrollPeriods
      parameters:
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Page index.
        example: 0
      - name: size
        in: query
        description: Page size.
        example: 20
      responses:
        "200":
          description: Paged payroll periods.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Payroll
      summary: Create payroll period
      description: Creates a payroll period.
      operationId: registerPeriod
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPayrollPeriodRequest"
            example:
              frequency: WEEKLY
              startDate: 2026-04-01
              endDate: 2026-04-07
        required: true
      responses:
        "201":
          description: Period created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPeriodResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/payments:
    get:
      tags:
      - Payroll
      summary: List payroll payments
      description: Returns payroll payment history.
      operationId: listPayrollPayments
      parameters:
      - name: employeeId
        in: query
        description: Filter by employee id.
        required: false
        schema:
          type: integer
          format: int64
        example: 101
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Page index.
        example: 0
      - name: size
        in: query
        description: Page size.
        example: 20
      responses:
        "200":
          description: Paged payroll payments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Payroll
      summary: Register payroll payment
      description: Registers a payroll payment and optional debt if payment is partial.
      operationId: registerPayrollPayment
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPayrollPaymentRequest"
            example:
              employeeId: 101
              frequency: WEEKLY
              workedDaysStart: 2026-04-01
              workedDaysEnd: 2026-04-07
              grossAmount: 3500.0
              netAmount: 3350.0
              destinationAccount: "012180001234567890"
              transactionId: TX-2026-000134
        required: true
      responses:
        "201":
          description: Payment registered.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollPaymentResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/import:
    post:
      tags:
      - Payroll
      summary: Import payroll records
      description: Bulk create/update payroll records from .xlsx using optional scope
        filters.
      operationId: importPayrollRecords
      parameters:
      - name: page
        in: query
        description: Page index (same as export listing).
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size.
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      - name: employeeId
        in: query
        description: Restrict import to employee id.
        example: 101
      - name: periodId
        in: query
        description: Restrict import to period id.
        example: 22
      - name: from
        in: query
        description: Worked start date (inclusive).
        example: 2026-04-01
      - name: to
        in: query
        description: Worked end date (inclusive).
        example: 2026-04-30
      requestBody:
        description: "Multipart: required part **file** (`.xlsx`). Optional query-style\
          \ fields `page`, `size`, `employeeId`, `periodId`, `from`, `to` scope the\
          \ import (same as export)."
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
            encoding:
              file:
                contentType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        required: true
      responses:
        "200":
          description: Import counters and row errors.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpreadsheetBulkImportResult"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities:
    get:
      tags:
      - CRM — Opportunities
      summary: Search opportunities
      description: "Filtered, paginated list. Response is **PagedResponse**. Rate\
        \ limit: **READ_HEAVY**."
      operationId: searchOpportunities
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/OpportunitySearchRequest"
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size (1–100).
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      - name: status
        in: query
        description: Filter by pipeline status.
        schema:
          type: string
          enum:
          - NEW
          - DISCOVERY
          - PROPOSAL
          - NEGOTIATION
          - WON
          - LOST
          - ABANDONED
        example: NEGOTIATION
      - name: companyNameContains
        in: query
        description: Case-insensitive company name substring.
        example: Acme
      - name: titleContains
        in: query
        description: Case-insensitive title substring.
        example: rollout
      responses:
        "200":
          description: Paged opportunities.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - CRM — Opportunities
      summary: Create opportunity
      description: Registers a new opportunity from **CreateOpportunityRequest** (Jakarta
        validation on the body).
      operationId: createOpportunity
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOpportunityRequest"
            examples:
              minimal:
                description: minimal
                value:
                  title: Enterprise rollout
                  description: Phased deployment
                  contactName: Jane Doe
                  contactEmail: jane@example.com
                  companyName: Acme Corp
                  estimatedValue: 150000.0
                  probabilityPercent: 25
                  source: INBOUND
        required: true
      responses:
        "201":
          description: Created opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/win:
    post:
      tags:
      - CRM — Opportunities
      summary: Mark opportunity won
      description: "Won outcome with `WinOpportunityRequest`. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: winOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WinOpportunityRequest"
        required: true
      responses:
        "200":
          description: Closed-won opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/tasks:
    get:
      tags:
      - CRM — Opportunities
      summary: List tasks for an opportunity
      description: "Validates the opportunity exists, then returns tasks filtered\
        \ by `TaskSearchRequest` (includes `opportunityId`). Rate limit: **READ_HEAVY**."
      operationId: listOpportunityTasks
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/TaskSearchRequest"
      responses:
        "200":
          description: Paged tasks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "404":
          description: Opportunity not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - CRM — Opportunities
      summary: Create task under opportunity
      description: Creates a task linked to the opportunity **id** after verifying
        the opportunity exists. The request body is the same **TaskRequest** as `POST
        /api/v1/tasks`; the opportunity id on the path takes precedence for linkage.
      operationId: createTaskForOpportunity
      parameters:
      - name: id
        in: path
        description: Opportunity id.
        required: true
        schema:
          type: integer
          format: int64
        example: 500
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskRequest"
            examples:
              taskUnderOpportunity:
                description: taskUnderOpportunity
                value:
                  title: Send proposal
                  priority: MEDIUM
                  dueDate: 2026-04-22T12:00:00
        required: true
      responses:
        "201":
          description: Created task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "404":
          description: Opportunity not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/reopen:
    post:
      tags:
      - CRM — Opportunities
      summary: Reopen opportunity
      description: Returns an opportunity to an open pipeline state when allowed.
      operationId: reopenOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/pipeline/proposal:
    post:
      tags:
      - CRM — Opportunities
      summary: Send proposal
      description: Pipeline transition to proposal stage.
      operationId: sendProposal
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/pipeline/negotiation:
    post:
      tags:
      - CRM — Opportunities
      summary: Start negotiation
      description: Pipeline transition to negotiation.
      operationId: startNegotiation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/pipeline/discovery:
    post:
      tags:
      - CRM — Opportunities
      summary: Move opportunity to discovery
      description: Pipeline transition; domain rules apply (invalid state → **400**).
      operationId: moveToDiscovery
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/lose:
    post:
      tags:
      - CRM — Opportunities
      summary: Mark opportunity lost
      description: "Lost outcome with reason. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: loseOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoseOpportunityRequest"
        required: true
      responses:
        "200":
          description: Closed-lost opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/abandon:
    post:
      tags:
      - CRM — Opportunities
      summary: Abandon opportunity
      description: "Abandoned state. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: abandonOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/import:
    post:
      tags:
      - CRM — Opportunities
      summary: Import opportunities from spreadsheet
      description: "Upload `multipart/form-data` with field **file** (`.xlsx`). Empty\
        \ file yields **400**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: importOpportunities
      requestBody:
        description: "Form part **file**: Excel workbook (.xlsx)."
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include part `file` with the Excel binary.
              required:
              - file
            encoding:
              file:
                contentType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        required: true
      responses:
        "200":
          description: Row-level import outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpreadsheetBulkImportResult"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/{id}/submit:
    post:
      tags:
      - Inventory — transactions
      summary: Submit transaction for approval
      description: "Sin cuerpo. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: submit
      parameters:
      - name: id
        in: path
        description: ID de transacción.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/{id}/complete:
    post:
      tags:
      - Inventory — transactions
      summary: Complete transaction
      description: "Sin cuerpo. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: complete_1
      parameters:
      - name: id
        in: path
        description: ID de transacción.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/{id}/cancel:
    post:
      tags:
      - Inventory — transactions
      summary: Cancel transaction
      description: "**204** sin cuerpo si ok. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: cancel_1
      parameters:
      - name: id
        in: path
        description: ID de transacción.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      responses:
        "204":
          description: Cancelada.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/{id}/approve:
    post:
      tags:
      - Inventory — transactions
      summary: Approve transaction
      description: "Cuerpo JSON **ApproveTransactionRequest** (`approvedById`). Rate\
        \ limit: **SENSITIVE_OPERATIONS**."
      operationId: approve
      parameters:
      - name: id
        in: path
        description: ID de transacción.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/transfer:
    post:
      tags:
      - Inventory — transactions
      summary: Register transfer transaction
      description: "Cuerpo JSON **TransferTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: transfer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransferTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/scrap:
    post:
      tags:
      - Inventory — transactions
      summary: Register scrap transaction
      description: "Cuerpo JSON **ScrapTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: scrap
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScrapTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/sale:
    post:
      tags:
      - Inventory — transactions
      summary: Register sale transaction
      description: "Cuerpo JSON **SaleTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: sale
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaleTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/return-supplier:
    post:
      tags:
      - Inventory — transactions
      summary: Register supplier return transaction
      description: "Cuerpo JSON **ReturnSupplierTransactionRequest**. Rate limit:\
        \ **SENSITIVE_OPERATIONS**."
      operationId: returnToSupplier
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnSupplierTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/return-client:
    post:
      tags:
      - Inventory — transactions
      summary: Register client return transaction
      description: "Cuerpo JSON **ReturnClientTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: returnFromClient
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnClientTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/purchase:
    post:
      tags:
      - Inventory — transactions
      summary: Register purchase transaction
      description: "Cuerpo JSON **PurchaseTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: purchase
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/physical-adjustment:
    post:
      tags:
      - Inventory — transactions
      summary: Register physical count adjustment
      description: "Cuerpo JSON **PhysicalAdjustmentTransactionRequest**. Rate limit:\
        \ **SENSITIVE_OPERATIONS**."
      operationId: physicalAdjustment
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhysicalAdjustmentTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/adjustment:
    post:
      tags:
      - Inventory — transactions
      summary: Register adjustment transaction
      description: "Cuerpo JSON **AdjustmentTransactionRequest**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: adjustment
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdjustmentTransactionRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock:
    get:
      tags:
      - Inventory — stock
      summary: Search stock rows
      description: "Búsqueda paginada de filas de existencia. Rate limit: **READ_HEAVY**."
      operationId: searchStock
      parameters:
      - name: itemId
        in: query
        description: Filtrar por artículo.
        required: false
        schema:
          type: integer
          format: int64
        example: 10
      - name: locationId
        in: query
        description: Filtrar por ubicación.
        required: false
        schema:
          type: integer
          format: int64
        example: 3
      - name: status
        in: query
        description: Estado del registro de inventario.
        required: false
        schema:
          type: string
          enum:
          - NORMAL
          - LOW_STOCK
          - OUT_OF_STOCK
          - OVERSTOCKED
      - name: page
        in: query
        description: Índice de página (base 0).
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Página de existencias.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Inventory — stock
      summary: Create initial stock
      description: "Crea la primera existencia artículo+ubicación. **201** si ok.\
        \ Puede fallar con `INVENTORY_ALREADY_EXISTS` o referencias inválidas. Rate\
        \ limit: **SENSITIVE_OPERATIONS**."
      operationId: createInitialStock
      requestBody:
        description: "JSON (**CreateInitialStockRequest**): `itemId`, `locationId`,\
          \ `initialQuantity`."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInitialStockRequest"
        required: true
      responses:
        "201":
          description: Fila de inventario creada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryStockResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/locations:
    get:
      tags:
      - Inventory — storage locations
      summary: Search storage locations
      description: "Lista paginada. Rate limit: **READ_HEAVY**."
      operationId: searchLocations
      parameters:
      - name: type
        in: query
        required: false
        schema:
          type: string
          enum:
          - WAREHOUSE
          - ZONE
          - AISLE
          - SHELF
          - BIN
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - ACTIVE
          - FULL
          - BLOCKED
          - INACTIVE
      - name: page
        in: query
        description: Índice de página basado en cero.
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (máximo 100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Inventory — storage locations
      summary: Create storage location
      description: "JSON **StorageLocationCreateRequest**. **201**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: createLocation
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StorageLocationCreateRequest"
        required: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/items:
    get:
      tags:
      - Inventory — items
      summary: Search inventory items
      description: "Lista paginada con filtros opcionales. Respuesta: **PagedResponse**.\
        \ Rate limit: **READ_HEAVY**."
      operationId: searchItems
      parameters:
      - name: name
        in: query
        description: Filtro por nombre (coincidencia parcial).
        required: false
        schema:
          type: string
      - name: sku
        in: query
        description: Filtro por SKU.
        required: false
        schema:
          type: string
      - name: category
        in: query
        description: Filtro por categoría.
        required: false
        schema:
          type: string
          enum:
          - RAW_MATERIAL
          - FINISHED_GOOD
          - CONSUMABLE
          - SPARE_PART
          - PACKAGING
          - TOOL
          - MACHINE
          - FURNITURE
          - OTHER
      - name: status
        in: query
        description: Filtro por estado del artículo.
        required: false
        schema:
          type: string
          enum:
          - ACTIVE
          - DISCONTINUED
          - OUT_OF_STOCK
          - PENDING_APPROVAL
      - name: page
        in: query
        description: Índice de página (base 0).
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Página de artículos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Inventory — items
      summary: Create inventory item
      description: "Registra un artículo. **201** con cuerpo JSON. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: createItem
      requestBody:
        description: Cuerpo JSON (**ItemCreateRequest**).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemCreateRequest"
        required: true
      responses:
        "201":
          description: Artículo creado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters:
    get:
      tags:
      - Operations — Headquarters
      summary: List headquarters
      description: "Paginated list. JSON follows Spring Data **`Page`**: `content`\
        \ is an array of **HeadQuarterResponse**; also includes `totalElements`, `totalPages`,\
        \ `number`, `size`, etc. Rate limit: **READ_HEAVY**."
      operationId: getAllHeadquarters
      parameters:
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size (1–100).
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Page of headquarters (`Page<HeadQuarterResponse>`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Operations — Headquarters
      summary: Create headquarter
      description: Registers a new site from **HeadQuarterRequest** (Jakarta format
        validation on the body).
      operationId: createHeadquarter
      requestBody:
        description: JSON body; **name** is required. **address** and **description**
          are optional.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeadQuarterRequest"
            examples:
              full:
                summary: All fields
                description: full
                value:
                  name: North Plant — Monterrey
                  address: "1200 Industrial Ave, Industrial Park"
                  description: Raw materials receiving; dock 3 access.
              minimal:
                summary: Name only
                description: minimal
                value:
                  name: Guadalajara DC
        required: true
      responses:
        "201":
          description: Created headquarter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeadQuarterResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters/import:
    post:
      tags:
      - Operations — Headquarters
      summary: Import headquarters from spreadsheet
      description: "Upload `multipart/form-data` with field **file** (`.xlsx`). Empty\
        \ file yields **400**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: importHeadquarters
      requestBody:
        description: "Form part **file**: Excel workbook (.xlsx)."
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include part `file` with the Excel binary.
              required:
              - file
            encoding:
              file:
                contentType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        required: true
      responses:
        "200":
          description: Row-level import outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpreadsheetBulkImportResult"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/resources/upload:
    post:
      tags:
      - File resources
      summary: Upload module resource file
      description: "Upload a file scoped to a specific module and optional entity\
        \ (always stored under `pimienta/sources/resources/{module}/{entityId?}/`).\
        \ `module` is required. Max file size: **50 MB**. **ROLE_ADMIN** or **ROLE_MANAGER**.\
        \ Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: upload
      parameters:
      - name: module
        in: query
        description: "Module slug that owns this resource file (e.g. `inventory`,\
          \ `crm`, `payroll`)."
        required: true
        schema:
          type: string
          example: inventory
      - name: entityType
        in: query
        description: Optional domain entity-type tag within the module (e.g. `inventory-item`).
        required: false
        schema:
          type: string
          example: inventory-item
      - name: entityId
        in: query
        description: Optional entity id; creates a sub-folder in S3 for easy grouping.
        required: false
        schema:
          type: integer
          format: int64
          example: 42
      - name: description
        in: query
        description: Human-readable note about the file.
        required: false
        schema:
          type: string
          example: Stock report for warehouse A
      - name: uploadedByUserId
        in: query
        description: Id of the user performing the upload (audit).
        required: false
        schema:
          type: integer
          format: int64
          example: 5
      requestBody:
        description: Multipart body — the `file` part carries the raw binary.
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include the `file` part. All other params are query
                params.
              required:
              - file
            encoding:
              file:
                contentType: application/octet-stream
        required: true
      responses:
        "201":
          description: Resource file stored in S3 and catalog entry created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileAssetResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/management/upload:
    post:
      tags:
      - File management
      summary: Upload file (admin)
      description: "Upload any company file to S3 (`pimienta/sources/`). Send `multipart/form-data`\
        \ with the binary `file` part plus query parameters. `category` is required\
        \ for all uploads; `module` is additionally required when `category = RESOURCE`.\
        \ Max file size: **50 MB**. **ROLE_ADMIN** only. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: upload_1
      parameters:
      - name: category
        in: query
        description: "Storage category: `TEMPLATE` | `COMPANY` | `EXTRAS` | `RESOURCE`."
        required: true
        schema:
          type: string
          enum:
          - TEMPLATE
          - COMPANY
          - EXTRAS
          - RESOURCE
          example: TEMPLATE
      - name: module
        in: query
        description: "Module slug — **required when `category = RESOURCE`** (e.g.\
          \ `inventory`, `crm`)."
        required: false
        schema:
          type: string
          example: inventory
      - name: entityType
        in: query
        description: Optional entity-type tag within the module (e.g. `inventory-item`).
        required: false
        schema:
          type: string
          example: inventory-item
      - name: entityId
        in: query
        description: Optional entity id the file is linked to.
        required: false
        schema:
          type: integer
          format: int64
          example: 42
      - name: description
        in: query
        description: Human-readable note about the file.
        required: false
        schema:
          type: string
          example: Q2 invoice template
      - name: uploadedByUserId
        in: query
        description: Id of the user performing the upload (for audit purposes).
        required: false
        schema:
          type: integer
          format: int64
          example: 1
      requestBody:
        description: Multipart body — the `file` part carries the raw binary.
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include the `file` part. All other params are query
                params.
              required:
              - file
            encoding:
              file:
                contentType: application/octet-stream
        required: true
      responses:
        "201":
          description: File stored in S3 and catalog entry created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileAssetResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees:
    get:
      tags:
      - HR — Employees
      summary: Search employees
      description: "Filtered, paginated list: optional `status`, `department`, `q`\
        \ (search), plus pagination via `PageableRequest`. Rate limit: **READ_HEAVY**."
      operationId: searchEmployees
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - DRAFT
          - PENDING_CONTRACT
          - ACTIVE
          - SICK
          - ON_VACATION
          - ON_LEAVE
          - TERMINATED
          - FIRED
          - RESIGNED
          - UNDEFINED
      - name: department
        in: query
        required: false
        schema:
          type: string
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: page
        in: query
        description: Índice de página basado en cero.
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (máximo 100).
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Paged employee rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - HR — Employees
      summary: Register employee
      description: |-
        Recommended: **`multipart/form-data`** parts:

        + **`employee`**: JSON matching **`RegisterEmployeeRequest`** (`Content-Type`:
          **`application/json`** when possible).
        + **`photo`** (optional): image (`image/jpeg`, `image/png`, …).

        Browsers/Swagger may label **`employee`** as **`application/octet-stream`**; the server parses
        the bytes as UTF-8 JSON anyway. Do not send onboarding fields only as URL query strings.

        For JSON-without-photo only, **`application/json`** is also accepted (alternate operation hidden
        in Swagger). Rate limit: **SENSITIVE_OPERATIONS**.
      operationId: registerEmployee
      requestBody:
        description: "Must be **multipart/form-data** with parts **employee** (JSON,\
          \ RegisterEmployeeRequest) and optional **photo** (file)."
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/EmployeeRegisterMultipartBody"
              description: "Two form parts: **employee** (object) and optional **photo**\
                \ (binary)."
              required:
              - employee
            encoding:
              employee:
                contentType: application/json
              photo: {}
        required: true
      responses:
        "201":
          description: Created employee.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{employeeId}/attendance/start-workday:
    post:
      tags:
      - HR — Employee attendance
      summary: Start workday (check-in)
      description: |-
        Recommended: **`multipart/form-data`** parts:

        + **`attendance`**: JSON matching **`StartWorkdayRequest`** (`Content-Type`:
          **`application/json`** when possible).
        + **`checkInEvidencePhoto`** (optional): image (`image/jpeg`, `image/png`, …). The server stores the object in S3 and persists the **object key**; API responses return a **time-limited HTTPS URL**.

        Browsers/Swagger may label **`attendance`** as **`application/octet-stream`**; the server parses the bytes as UTF-8 JSON anyway.

        For JSON **without** evidence photo only, **`application/json`** is also accepted (alternate operation hidden in Swagger). **409** if an open attendance already exists for the resolved work date (`ATTENDANCE_ALREADY_OPEN`). **404** for unknown employee or headquarter.
      operationId: startWorkdayMultipart
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        description: "Must be **multipart/form-data** with part **attendance** (JSON,\
          \ StartWorkdayRequest) and optional **checkInEvidencePhoto** (file)."
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttendanceStartMultipartBody"
              description: "Two form parts: **attendance** (object) and optional **checkInEvidencePhoto**\
                \ (binary)."
              required:
              - attendance
            encoding:
              attendance:
                contentType: application/json
              checkInEvidencePhoto: {}
        required: true
      responses:
        "201":
          description: Created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "409":
          description: Conflict.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{employeeId}/attendance/end-workday:
    post:
      tags:
      - HR — Employee attendance
      summary: End workday (check-out)
      description: |-
        Recommended: **`multipart/form-data`** parts:

        + **`attendance`**: JSON matching **`EndWorkdayRequest`** (`Content-Type`:
          **`application/json`** when possible).
        + **`checkOutEvidencePhoto`** (optional): image. Stored in S3; responses use **presigned HTTPS URLs**.

        For JSON **without** evidence photo only, **`application/json`** is also accepted (hidden in Swagger). **409** when no open attendance exists (`ATTENDANCE_NOT_OPEN_FOR_CHECKOUT`).
      operationId: endWorkdayMultipart
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        description: "Must be **multipart/form-data** with part **attendance** (JSON,\
          \ EndWorkdayRequest) and optional **checkOutEvidencePhoto** (file)."
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AttendanceEndMultipartBody"
              description: "Two form parts: **attendance** (object) and optional **checkOutEvidencePhoto**\
                \ (binary)."
              required:
              - attendance
            encoding:
              attendance:
                contentType: application/json
              checkOutEvidencePhoto: {}
        required: true
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceResponse"
        "409":
          description: Conflict.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/import:
    post:
      tags:
      - HR — Employees
      summary: Import employees from spreadsheet
      description: "Upload `multipart/form-data` with field **file** (`.xlsx`). Empty\
        \ file yields **400**. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: importEmployees
      requestBody:
        description: "Form part **file**: Excel workbook (.xlsx)."
        content:
          multipart/form-data:
            schema:
              type: object
              description: Must include part `file` with the Excel binary.
              required:
              - file
            encoding:
              file:
                contentType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        required: true
      responses:
        "200":
          description: Row-level import outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpreadsheetBulkImportResult"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/contracts:
    get:
      tags:
      - Contracts
      summary: List active contracts
      description: "Returns a page of **non–soft-deleted** contracts. Pagination via\
        \ `page`, `size` (and optional sort if configured). Rate limit: **READ_HEAVY**."
      operationId: listContracts
      parameters:
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Índice de página (desde 0).
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Paged list of contracts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    post:
      tags:
      - Contracts
      summary: Create contract
      description: "Registers a new contract. **EMPLOYEE** category requires `employeeId`.\
        \ For **FIXED_TERM**, `effectiveEnd` is required. **INDEFINITE** must not\
        \ set `effectiveEnd`. May activate related employment when category is EMPLOYEE.\
        \ Rate limit: **STANDARD** (class default)."
      operationId: createContract
      requestBody:
        description: Cuerpo JSON del contrato a registrar (véase esquema **CreateContractRequest**).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContractRequest"
            examples:
              supplierFixedTerm:
                summary: "Proveedor, plazo fijo"
                description: supplierFixedTerm
                value:
                  name: Contrato suministro materias primas 2025
                  description: Acuerdo marco con proveedor homologado.
                  category: SUPPLIER
                  employeeId: null
                  opportunityId: 5001
                  projectId: 88
                  termKind: FIXED_TERM
                  effectiveStart: 2025-05-01
                  effectiveEnd: 2026-04-30
                  documentUrl: https://storage.example.com/contracts/sup-2025-001.pdf
                  termsAndConditions: Pago a 30 días; penalizaciones según cláusula
                    8.
                  referenceCode: CT-SUP-2025-042
                  renewalCycleMonths: 12
                  agreedValue: 250000.0
                  currencyCode: MXN
              employeeIndefinite:
                summary: "Laboral, tiempo indefinido"
                description: employeeIndefinite
                value:
                  name: Contrato individual de trabajo — Ana Ruiz
                  description: "Operadora de línea, planta norte."
                  category: EMPLOYEE
                  employeeId: 101
                  opportunityId: null
                  projectId: null
                  termKind: INDEFINITE
                  effectiveStart: 2025-06-01
                  documentUrl: https://storage.example.com/contracts/emp-101.pdf
                  termsAndConditions: null
                  referenceCode: LAB-2025-101
                  renewalCycleMonths: null
                  agreedValue: null
                  currencyCode: null
        required: true
      responses:
        "201":
          description: Contract created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContractResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/contracts/{id}/renew:
    post:
      tags:
      - Contracts
      summary: Renew fixed-term contract
      description: "Advances `effectiveEnd` by `renewalCycleMonths` for **FIXED_TERM**\
        \ contracts. **400** if term kind, cycle, or end date is invalid (`ContractRuleViolationException`)."
      operationId: renewContract
      parameters:
      - name: id
        in: path
        description: Contrato de plazo fijo a renovar (suma `renewalCycleMonths` al
          fin actual).
        required: true
        schema:
          type: integer
          format: int64
        example: 42
      responses:
        "200":
          description: Contract after renewal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContractResponse"
        "404":
          description: Contract not found (`CONTRACT_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/auth/register:
    post:
      tags:
      - Authentication
      summary: Register a new account
      description: "Creates a user in **pending approval** (`PENDING_APPROVAL`). No\
        \ access token is issued until an administrator approves the account. Rate\
        \ limit: **STRICT**."
      operationId: register
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
        required: true
      responses:
        "201":
          description: Account created; response explains pending approval.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
  /api/v1/auth/refresh:
    post:
      tags:
      - Authentication
      summary: Refresh access token
      description: "Issues a new access token (and refresh token when rotated) using\
        \ the **refresh token** in the body. Fails with `INVALID_REFRESH_TOKEN` when\
        \ the refresh token is unknown or expired. Rate limit: **AUTH_SESSION**."
      operationId: refresh
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
        required: true
      responses:
        "200":
          description: Newly issued tokens.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
  /api/v1/auth/logout:
    post:
      tags:
      - Authentication
      summary: Sign out
      description: "Revokes the refresh session (best-effort; idempotent). Optional\
        \ body may carry refresh token hints for the server-side store. Rate limit:\
        \ **AUTH_SESSION**."
      operationId: logout
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutRequest"
      responses:
        "204":
          description: Session revoked; no response body.
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
  /api/v1/auth/login:
    post:
      tags:
      - Authentication
      summary: Sign in
      description: "Authenticates with email and password and returns **access** and\
        \ **refresh** tokens. Common `errorCode` values: `AUTHENTICATION_FAILED`,\
        \ `USER_BANNED`, `ACCOUNT_PENDING_APPROVAL`. Rate limit: **STRICT**."
      operationId: login
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
        required: true
      responses:
        "200":
          description: Issued access and refresh tokens.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
  /api/v1/users/me:
    get:
      tags:
      - Account — Profile
      summary: Get my profile
      description: "Returns the authenticated user's profile. **401** if the token\
        \ is missing/invalid. Rate limit: **READ_HEAVY**."
      operationId: getProfile
      responses:
        "200":
          description: Current user profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    patch:
      tags:
      - Account — Profile
      summary: Update my profile
      description: Partially updates profile fields from the JSON body. Requires a
        valid access token.
      operationId: updateProfile
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProfileRequest"
        required: true
      responses:
        "200":
          description: Updated profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/{id}/status:
    patch:
      tags:
      - Operations — Tasks
      summary: Update task status
      description: "Applies **StatusUpdateRequest**. When status becomes COMPLETED,\
        \ completion timestamp is set in the use case layer. **404** if task not found\
        \ (`TASK_NOT_FOUND`)."
      operationId: updateStatus
      parameters:
      - name: id
        in: path
        description: Task id.
        required: true
        schema:
          type: integer
          format: int64
        example: 101
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StatusUpdateRequest"
            examples:
              complete:
                description: complete
                value:
                  status: COMPLETED
        required: true
      responses:
        "200":
          description: Updated task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/{id}/checklist/{itemOrder}/toggle:
    patch:
      tags:
      - Operations — Tasks
      summary: Toggle checklist item
      description: Flips completion for the checklist row identified by **itemOrder**
        (same key used when creating lines). **404** may indicate `TASK_NOT_FOUND`
        or `TASK_CHECKLIST_ITEM_NOT_FOUND`.
      operationId: toggleChecklistItem
      parameters:
      - name: id
        in: path
        description: Task id.
        required: true
        schema:
          type: integer
          format: int64
        example: 101
      - name: itemOrder
        in: path
        description: Checklist display order / line key.
        required: true
        schema:
          type: integer
          format: int32
        example: 0
      responses:
        "200":
          description: Updated task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "404":
          description: Task or checklist item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/{id}/assign:
    patch:
      tags:
      - Operations — Tasks
      summary: Assign task to employee
      description: Sets assignee from **AssignTaskRequest**. **404** if task not found
        (`TASK_NOT_FOUND`).
      operationId: assignTask
      parameters:
      - name: id
        in: path
        description: Task id.
        required: true
        schema:
          type: integer
          format: int64
        example: 101
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignTaskRequest"
            examples:
              assign:
                description: assign
                value:
                  employeeId: 42
        required: true
      responses:
        "200":
          description: Updated task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{projectId}/milestones/{milestoneId}:
    get:
      tags:
      - CRM — Project milestones
      summary: Get milestone by id
      description: '**404** if project or milestone missing.'
      operationId: getMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - CRM — Project milestones
      summary: Delete milestone
      description: "**204** on success. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: deleteMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No content.
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    patch:
      tags:
      - CRM — Project milestones
      summary: Update milestone
      description: Partial update. **404** if not found.
      operationId: updateMilestone
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: milestoneId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectMilestoneRequest"
        required: true
      responses:
        "200":
          description: Updated milestone.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMilestoneResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}:
    get:
      tags:
      - CRM — Projects
      summary: Get project by id
      description: '**404** if not found (`PROJECT_NOT_FOUND`).'
      operationId: getProject
      parameters:
      - name: id
        in: path
        description: Project id.
        required: true
        schema:
          type: integer
          format: int64
        example: 10
      responses:
        "200":
          description: Project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - CRM — Projects
      summary: Delete project
      description: "Deletes per domain rules. **204** on success. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: deleteProject
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No content.
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    patch:
      tags:
      - CRM — Projects
      summary: Update project
      description: Partial update. **404** if not found.
      operationId: updateProject
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectRequest"
        required: true
      responses:
        "200":
          description: Updated project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}:
    get:
      tags:
      - CRM — Opportunities
      summary: Get opportunity by id
      description: '**404** if not found (`OPPORTUNITY_NOT_FOUND`).'
      operationId: getOpportunity
      parameters:
      - name: id
        in: path
        description: Opportunity id.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      responses:
        "200":
          description: Opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - CRM — Opportunities
      summary: Delete opportunity
      description: "Soft-delete or remove per domain rules. **204** on success. Rate\
        \ limit: **SENSITIVE_OPERATIONS**."
      operationId: deleteOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No content.
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    patch:
      tags:
      - CRM — Opportunities
      summary: Update opportunity
      description: Partial update. **404** if not found.
      operationId: updateOpportunity
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOpportunityRequest"
        required: true
      responses:
        "200":
          description: Updated opportunity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v2/health:
    get:
      tags:
      - Health
      summary: Liveness check
      description: "Returns HTTP 200 when the API process is running. Does not validate\
        \ downstream dependencies (database, Redis). No Bearer token required."
      operationId: health
      responses:
        "200":
          description: Service is up.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
  /api/v1/users/me/dashboard:
    get:
      tags:
      - Account — Profile
      summary: Get my dashboard
      description: "Aggregated dashboard data for the authenticated user. Rate limit:\
        \ **READ_HEAVY**."
      operationId: getDashboard
      responses:
        "200":
          description: Dashboard payload for the current user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDashboardResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management:
    get:
      tags:
      - Account — User management
      summary: List users
      description: "Paginated directory of users. Accepts standard page/size/sort\
        \ via query parameters (`PageableRequest`). Rate limit: **READ_HEAVY**."
      operationId: listUsers
      parameters:
      - name: pageRequest
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      responses:
        "200":
          description: Paged list of users.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/{id}:
    get:
      tags:
      - Account — User management
      summary: Get user by id
      description: Loads a single user by numeric id. **404** if no user exists.
      operationId: getUserById
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: User found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "404":
          description: User not found (`USER_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/statistics:
    get:
      tags:
      - Account — User management
      summary: User statistics
      description: "Returns aggregate user counts for management dashboards. Rate\
        \ limit: **READ_HEAVY**."
      operationId: getStatistics
      responses:
        "200":
          description: Aggregated statistics.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserStatisticsResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/users/management/by-email:
    get:
      tags:
      - Account — User management
      summary: Get user by email
      description: Looks up a user by email (normalized on the server). **404** if
        unknown.
      operationId: getUserByEmail
      parameters:
      - name: email
        in: query
        required: true
        schema:
          type: string
          format: email
      responses:
        "200":
          description: User found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "404":
          description: User not found (`USER_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/{id}:
    get:
      tags:
      - Operations — Tasks
      summary: Get task by id
      description: '**404** if not found (`TASK_NOT_FOUND`).'
      operationId: getTaskById
      parameters:
      - name: id
        in: path
        description: Task id.
        required: true
        schema:
          type: integer
          format: int64
        example: 101
      responses:
        "200":
          description: Task detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - Operations — Tasks
      summary: Soft-delete task
      description: "Soft-deletes the task. **204** on success. Rate limit: **SENSITIVE_OPERATIONS**.\
        \ **404** if not found."
      operationId: deleteTask
      parameters:
      - name: id
        in: path
        description: Task id.
        required: true
        schema:
          type: integer
          format: int64
        example: 101
      responses:
        "204":
          description: No content; task soft-deleted.
        "404":
          description: Not found (`TASK_NOT_FOUND`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/tasks/export:
    get:
      tags:
      - Operations — Tasks
      summary: Export tasks to Excel
      description: "Streams an `.xlsx` report using the same optional filters as search;\
        \ all matching rows are exported. `Content-Disposition` suggests a filename.\
        \ Rate limit: **READ_HEAVY**."
      operationId: exportTasksToExcel
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/TaskSearchRequest"
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/Pageable"
      - name: page
        in: query
        description: Present for Spring binding; export is not limited to one page.
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Present for Spring binding; export ignores single-page limits.
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      - name: headquarterId
        in: query
        description: Filter by headquarter id.
        schema:
          type: integer
          format: int64
        example: 1
      - name: projectId
        in: query
        description: Filter by project id.
        schema:
          type: integer
          format: int64
        example: 10
      - name: opportunityId
        in: query
        description: Filter by CRM opportunity id.
        schema:
          type: integer
          format: int64
        example: 500
      - name: employeeId
        in: query
        description: Filter by assignee (employee) id.
        schema:
          type: integer
          format: int64
        example: 7
      - name: status
        in: query
        description: Filter by workflow status.
        schema:
          type: string
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
          - DELAYED
          - ON_HOLD
          - FAILED
          - UNDEFINED
        example: PENDING
      responses:
        "200":
          description: Excel workbook bytes.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/tasks:
    get:
      tags:
      - CRM — Projects
      summary: List tasks for a project
      description: "Validates project exists; filters tasks by `TaskSearchRequest`\
        \ with `projectId`. Rate limit: **READ_HEAVY**."
      operationId: listProjectTasks
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/TaskSearchRequest"
      responses:
        "200":
          description: Paged tasks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/projects/{id}/summary:
    get:
      tags:
      - CRM — Projects
      summary: Get project summary
      description: '**404** if not found.'
      operationId: getProjectSummary
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectSummaryResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/summary:
    get:
      tags:
      - Payroll
      summary: Payroll summary
      description: Returns aggregated payroll totals for a date range.
      operationId: getPayrollSummary
      parameters:
      - name: from
        in: query
        description: Summary start date.
        required: false
        schema:
          type: string
          format: date
        example: 2026-04-01
      - name: to
        in: query
        description: Summary end date.
        required: false
        schema:
          type: string
          format: date
        example: 2026-04-30
      responses:
        "200":
          description: Summary payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollSummaryResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/export:
    get:
      tags:
      - Payroll
      summary: Export payroll records
      description: Exports payroll records to .xlsx using scope filters.
      operationId: exportPayrollRecords
      parameters:
      - name: request
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PayrollBulkScopeRequest"
      - name: employeeId
        in: query
        description: Filter by employee id.
        example: 101
      - name: periodId
        in: query
        description: Filter by period id.
        example: 22
      - name: from
        in: query
        description: Worked start date (inclusive).
        example: 2026-04-01
      - name: to
        in: query
        description: Worked end date (inclusive).
        example: 2026-04-30
      - name: page
        in: query
        description: Page index.
        example: 0
      - name: size
        in: query
        description: Page size.
        example: 20
      responses:
        "200":
          description: Excel file.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/payroll/debts:
    get:
      tags:
      - Payroll
      summary: List payroll debts
      description: Returns pending and settled payroll debts.
      operationId: listDebts
      parameters:
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Page index.
        example: 0
      - name: size
        in: query
        description: Page size.
        example: 20
      responses:
        "200":
          description: Paged payroll debts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/{id}/summary:
    get:
      tags:
      - CRM — Opportunities
      summary: Get opportunity summary
      description: Lightweight read model for dashboards. **404** if not found.
      operationId: getOpportunitySummary
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunitySummaryResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/opportunities/export:
    get:
      tags:
      - CRM — Opportunities
      summary: Export opportunities to Excel
      description: "Streams an `.xlsx` report using the same optional filters and\
        \ pagination as search. `Content-Disposition` suggests a filename. Rate limit:\
        \ **READ_HEAVY**."
      operationId: exportOpportunities
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/OpportunitySearchRequest"
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size (1–100).
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      - name: status
        in: query
        description: Filter by pipeline status.
        schema:
          type: string
          enum:
          - NEW
          - DISCOVERY
          - PROPOSAL
          - NEGOTIATION
          - WON
          - LOST
          - ABANDONED
        example: NEGOTIATION
      - name: companyNameContains
        in: query
        description: Case-insensitive company name substring.
        example: Acme
      - name: titleContains
        in: query
        description: Case-insensitive title substring.
        example: rollout
      responses:
        "200":
          description: Excel workbook bytes.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/notifications/management:
    get:
      tags:
      - Notification management
      summary: Search notifications (admin)
      description: "Paginated search over the notification audit log. All filters\
        \ are optional and combined with AND logic. Date-time parameters use ISO-8601\
        \ format (`2026-05-28T10:15:00`). **ROLE_ADMIN** only. Rate limit: **READ_HEAVY**."
      operationId: search
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/NotificationAdminSearchRequest"
      - name: id
        in: query
        description: Filter by exact notification UUID.
        schema:
          type: string
          format: uuid
          example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
      - name: channel
        in: query
        description: Filter by delivery channel.
        schema:
          type: string
          enum:
          - EMAIL
          - SMS
          - LOG
          example: LOG
      - name: type
        in: query
        description: Filter by notification type.
        schema:
          type: string
          enum:
          - ACCOUNT_PENDING_APPROVAL
          - UNDEFINED
          example: ACCOUNT_PENDING_APPROVAL
      - name: status
        in: query
        description: Filter by delivery status.
        schema:
          type: string
          enum:
          - PENDING
          - SENT
          - FAILED
          - SKIPPED
          example: SENT
      - name: recipientUserId
        in: query
        description: Filter by recipient user id.
        schema:
          type: integer
          format: int64
          example: 1
      - name: relatedUserId
        in: query
        description: Filter by related business user id (e.g. the registrant).
        schema:
          type: integer
          format: int64
          example: 42
      - name: correlationId
        in: query
        description: Correlation id contains (case-insensitive).
        schema:
          type: string
          example: account-pending
      - name: recipientEmailContains
        in: query
        description: Recipient email contains (case-insensitive).
        schema:
          type: string
          example: admin@
      - name: recipientPhoneContains
        in: query
        description: Recipient phone contains.
        schema:
          type: string
          example: "+57300"
      - name: subjectContains
        in: query
        description: Subject contains (case-insensitive).
        schema:
          type: string
          example: approval
      - name: bodyContains
        in: query
        description: Body contains (case-insensitive).
        schema:
          type: string
          example: registered
      - name: createdFrom
        in: query
        description: "Created at from (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-01T00:00:00
      - name: createdTo
        in: query
        description: "Created at to (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-28T23:59:59
      - name: sentFrom
        in: query
        description: "Sent at from (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-01T00:00:00
      - name: sentTo
        in: query
        description: "Sent at to (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-28T23:59:59
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
      - name: size
        in: query
        description: Page size (max 100).
        schema:
          type: integer
          default: 20
      responses:
        "200":
          description: Paged notification records.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/notifications/management/statistics:
    get:
      tags:
      - Notification management
      summary: Notification statistics (admin)
      description: "Summary counts by status, channel, and type for a required ISO-8601\
        \ interval (`from`, `to`). Maximum range 366 days. **ROLE_ADMIN** only. Rate\
        \ limit: **READ_HEAVY**."
      operationId: statistics
      parameters:
      - name: request
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/NotificationStatisticsRequest"
      responses:
        "200":
          description: Aggregated statistics.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationStatisticsResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/notifications/logs:
    get:
      tags:
      - Notification logs
      summary: Today's notification logs (manager)
      description: "Returns **LOG-channel** notifications created **today** (server\
        \ calendar day). The channel and date window are enforced server-side regardless\
        \ of any filter you pass. Additional optional filters narrow within that window.\
        \ **ROLE_MANAGER** or **ROLE_ADMIN**. Rate limit: **READ_HEAVY**."
      operationId: searchTodayLogs
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/NotificationManagerLogSearchRequest"
      - name: id
        in: query
        description: Filter by exact notification UUID.
        schema:
          type: string
          format: uuid
      - name: type
        in: query
        description: Filter by notification type.
        schema:
          type: string
          enum:
          - ACCOUNT_PENDING_APPROVAL
          - UNDEFINED
          example: ACCOUNT_PENDING_APPROVAL
      - name: status
        in: query
        description: Filter by delivery status.
        schema:
          type: string
          enum:
          - PENDING
          - SENT
          - FAILED
          - SKIPPED
          example: SENT
      - name: recipientUserId
        in: query
        description: Filter by recipient user id.
        schema:
          type: integer
          format: int64
          example: 1
      - name: relatedUserId
        in: query
        description: Filter by related business user id.
        schema:
          type: integer
          format: int64
          example: 42
      - name: correlationId
        in: query
        description: Correlation id contains (case-insensitive).
        schema:
          type: string
          example: account-pending
      - name: subjectContains
        in: query
        description: Subject contains (case-insensitive).
        schema:
          type: string
          example: approval
      - name: bodyContains
        in: query
        description: Body contains (case-insensitive).
        schema:
          type: string
          example: registered
      - name: sentFrom
        in: query
        description: "Sent at from (inclusive), within today, ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-28T08:00:00
      - name: sentTo
        in: query
        description: "Sent at to (inclusive), within today, ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-28T18:00:00
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
      - name: size
        in: query
        description: Page size (max 100).
        schema:
          type: integer
          default: 20
      responses:
        "200":
          description: Paged LOG-channel notification entries for today.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions:
    get:
      tags:
      - Inventory — transactions
      summary: Search inventory transactions
      description: "Lista paginada. Rate limit: **READ_HEAVY**."
      operationId: searchTransactions
      parameters:
      - name: type
        in: query
        required: false
        schema:
          type: string
          enum:
          - PURCHASE_RECEIPT
          - SALE_DISPATCH
          - INTERNAL_TRANSFER
          - PHYSICAL_COUNT
          - RETURN_FROM_CLIENT
          - RETURN_TO_SUPPLIER
          - PRODUCTION_ISSUE
          - SCRAP_WRITE_OFF
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - DRAFT
          - PENDING
          - APPROVED
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
      - name: fromDate
        in: query
        description: Inicio de rango (ISO-8601 date-time).
        required: false
        schema:
          type: string
          format: date-time
        example: 2025-01-01T00:00:00
      - name: toDate
        in: query
        description: Fin de rango (ISO-8601 date-time).
        required: false
        schema:
          type: string
          format: date-time
        example: 2025-12-31T23:59:59
      - name: initiatedById
        in: query
        description: Filtrar por usuario que inició.
        required: false
        schema:
          type: integer
          format: int64
      - name: page
        in: query
        description: Índice de página basado en cero.
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (máximo 100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Página de transacciones.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/transactions/{id}:
    get:
      tags:
      - Inventory — transactions
      summary: Get transaction by id
      description: "**404** (`INVENTORY_TRANSACTION_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getTransactionById
      parameters:
      - name: id
        in: path
        description: ID de transacción.
        required: true
        schema:
          type: integer
          format: int64
        example: 100
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryTransactionResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock/{id}:
    get:
      tags:
      - Inventory — stock
      summary: Get stock row by id
      description: "**404** (`INVENTORY_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getStockById
      parameters:
      - name: id
        in: path
        description: Identificador de la fila de inventario.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: Existencia.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryStockResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock/out-of-stock:
    get:
      tags:
      - Inventory — stock
      summary: List out-of-stock rows
      description: "Existencias en cantidad cero. Rate limit: **READ_HEAVY**."
      operationId: findOutOfStock
      parameters:
      - name: page
        in: query
        description: Índice de página (base 0).
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Página de existencias.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock/low-stock:
    get:
      tags:
      - Inventory — stock
      summary: List low-stock rows
      description: "Artículos bajo punto de reorden. Rate limit: **READ_HEAVY**."
      operationId: findLowStock
      parameters:
      - name: page
        in: query
        description: Índice de página (base 0).
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Página de existencias.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock/location/{locationId}:
    get:
      tags:
      - Inventory — stock
      summary: List stock by location
      description: "Existencias en una ubicación. Rate limit: **READ_HEAVY**."
      operationId: listByLocation
      parameters:
      - name: locationId
        in: path
        description: Identificador de la ubicación de almacén.
        required: true
        schema:
          type: integer
          format: int64
        example: 3
      responses:
        "200":
          description: Lista de existencias.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryStockResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/stock/item/{itemId}:
    get:
      tags:
      - Inventory — stock
      summary: List stock by item
      description: "Todas las filas de existencia para un artículo. Rate limit: **READ_HEAVY**."
      operationId: listByItem
      parameters:
      - name: itemId
        in: path
        description: Identificador del artículo.
        required: true
        schema:
          type: integer
          format: int64
        example: 10
      responses:
        "200":
          description: Lista de existencias.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryStockResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/movements:
    get:
      tags:
      - Inventory — movements
      summary: Search inventory movements
      description: "Lista paginada. Rate limit: **READ_HEAVY**."
      operationId: searchMovements
      parameters:
      - name: type
        in: query
        required: false
        schema:
          type: string
          enum:
          - PURCHASE
          - RETURN_FROM_CLIENT
          - INITIAL_STOCK
          - SALE
          - RETURN_TO_SUPPLIER
          - USAGE
          - SCRAP
          - TRANSFER
          - ADJUSTMENT_PLUS
          - ADJUSTMENT_MINUS
      - name: direction
        in: query
        required: false
        schema:
          type: string
          enum:
          - IN
          - OUT
          - NEUTRAL
      - name: itemId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: locationId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: fromDate
        in: query
        description: ISO-8601 date-time
        required: false
        schema:
          type: string
          format: date-time
      - name: toDate
        in: query
        description: ISO-8601 date-time
        required: false
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        description: Índice de página basado en cero.
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (máximo 100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/movements/{id}:
    get:
      tags:
      - Inventory — movements
      summary: Get movement by id
      description: "**404** (`INVENTORY_MOVEMENT_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getMovementById
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 500
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryMovementResponse"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/movements/location/{locationId}:
    get:
      tags:
      - Inventory — movements
      summary: List movements by location
      description: "Rate limit: **READ_HEAVY**."
      operationId: listByLocation_1
      parameters:
      - name: locationId
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 3
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryMovementResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/movements/item/{itemId}:
    get:
      tags:
      - Inventory — movements
      summary: List movements by item
      description: "Rate limit: **READ_HEAVY**."
      operationId: listByItem_1
      parameters:
      - name: itemId
        in: path
        required: true
        schema:
          type: integer
          format: int64
        example: 10
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryMovementResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/movements/by-reference:
    get:
      tags:
      - Inventory — movements
      summary: List movements by reference number
      description: "Rate limit: **READ_HEAVY**."
      operationId: findByReferenceNumber
      parameters:
      - name: referenceNumber
        in: query
        description: Número de referencia de la transacción u operación relacionada.
        required: true
        schema:
          type: string
        example: TX-2025-00042
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryMovementResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/locations/tree:
    get:
      tags:
      - Inventory — storage locations
      summary: Get location tree
      description: "Árbol completo de ubicaciones. Rate limit: **READ_HEAVY**."
      operationId: getLocationTree
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LocationTreeNodeResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/locations/parent/{parentId}/children:
    get:
      tags:
      - Inventory — storage locations
      summary: List child locations
      description: "Hijos directos. Rate limit: **READ_HEAVY**."
      operationId: getChildren
      parameters:
      - name: parentId
        in: path
        description: ID del padre.
        required: true
        schema:
          type: integer
          format: int64
        example: 1
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StorageLocationResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/inventory/items/lookup:
    get:
      tags:
      - Inventory — items
      summary: Lookup item by SKU or barcode
      description: "Devuelve un artículo por SKU o código de barras. **404** si no\
        \ existe (`ITEM_NOT_FOUND`). Rate limit: **READ_HEAVY**."
      operationId: getItemBySkuOrBarcode
      parameters:
      - name: q
        in: query
        description: SKU o código de barras a resolver (no vacío).
        required: true
        schema:
          type: string
          minLength: 1
        example: SKU-001
      responses:
        "200":
          description: Artículo encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemResponse"
        "404":
          description: No encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters/statistics:
    get:
      tags:
      - Operations — Headquarters
      summary: Headquarter statistics
      description: "Aggregated metrics for sites. Rate limit: **READ_HEAVY**."
      operationId: getHeadquarterStatistics
      responses:
        "200":
          description: Statistics payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeadquarterStatisticsResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters/name/{name}:
    get:
      tags:
      - Operations — Headquarters
      summary: Get headquarter by name
      description: '**404** if no match (`HEADQUARTER_NOT_FOUND`).'
      operationId: getHeadquarterByName
      parameters:
      - name: name
        in: path
        description: Exact stored headquarter name. URL-encode spaces or special characters
          (e.g. `North%20Plant`).
        required: true
        schema:
          type: string
        example: North-Plant-Monterrey
      responses:
        "200":
          description: Headquarter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeadQuarterResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/headquarters/export:
    get:
      tags:
      - Operations — Headquarters
      summary: Export headquarters to Excel
      description: "Streams an `.xlsx` report for the requested page. `Content-Disposition`\
        \ suggests a filename. Rate limit: **READ_HEAVY**."
      operationId: exportHeadquarters
      parameters:
      - name: pageableRequest
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      - name: page
        in: query
        description: Page index for the export batch (same binding model as list).
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Page size (1–100); controls how many rows each export chunk processes.
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 100
      responses:
        "200":
          description: Excel workbook bytes.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/resources:
    get:
      tags:
      - File resources
      summary: Search module resource files
      description: "Paginated search restricted to the **RESOURCE** category. `module`\
        \ is required — the server enforces it and returns 400 if absent. Optionally\
        \ narrow by entity type, entity id, filename, content type, or creation date\
        \ range. **ROLE_ADMIN** or **ROLE_MANAGER**. Rate limit: **READ_HEAVY**."
      operationId: search_1
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/FileResourceSearchRequest"
      - name: module
        in: query
        description: "Module slug to query (e.g. `inventory`, `crm`, `payroll`)."
        required: true
        schema:
          type: string
          example: inventory
      - name: entityType
        in: query
        description: Filter by entity-type tag.
        schema:
          type: string
          example: inventory-item
      - name: entityId
        in: query
        description: Filter by entity id.
        schema:
          type: integer
          format: int64
          example: 42
      - name: originalNameContains
        in: query
        description: Original filename contains (case-insensitive).
        schema:
          type: string
          example: report
      - name: contentTypeContains
        in: query
        description: MIME type contains (case-insensitive).
        schema:
          type: string
          example: pdf
      - name: createdFrom
        in: query
        description: "Created from (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-01T00:00:00
      - name: createdTo
        in: query
        description: "Created to (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-31T23:59:59
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
      - name: size
        in: query
        description: Page size (max 100).
        schema:
          type: integer
          default: 20
      responses:
        "200":
          description: Paged module resource files.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/resources/{id}/download-url:
    get:
      tags:
      - File resources
      summary: Generate resource download URL
      description: Returns a 24-hour pre-signed URL. **ROLE_ADMIN** or **ROLE_MANAGER**.
      operationId: downloadUrl
      parameters:
      - name: id
        in: path
        description: File asset UUID.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Pre-signed download URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDownloadUrlResponse"
        "404":
          description: Asset not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/management:
    get:
      tags:
      - File management
      summary: Search file assets (admin)
      description: "Paginated search across all file categories with optional filters\
        \ combined with AND logic. Date-time parameters use ISO-8601 format (`2026-05-28T10:15:00`).\
        \ **ROLE_ADMIN** only. Rate limit: **READ_HEAVY**."
      operationId: search_2
      parameters:
      - name: filter
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/FileAssetSearchRequest"
      - name: category
        in: query
        description: Filter by storage category.
        schema:
          type: string
          enum:
          - TEMPLATE
          - COMPANY
          - EXTRAS
          - RESOURCE
          example: TEMPLATE
      - name: module
        in: query
        description: Filter by module slug (RESOURCE category).
        schema:
          type: string
          example: inventory
      - name: entityType
        in: query
        description: Filter by entity-type tag.
        schema:
          type: string
          example: inventory-item
      - name: entityId
        in: query
        description: Filter by entity id.
        schema:
          type: integer
          format: int64
          example: 42
      - name: originalNameContains
        in: query
        description: Original filename contains (case-insensitive).
        schema:
          type: string
          example: invoice
      - name: contentTypeContains
        in: query
        description: MIME type contains (case-insensitive).
        schema:
          type: string
          example: spreadsheet
      - name: uploadedByUserId
        in: query
        description: Filter by uploader user id.
        schema:
          type: integer
          format: int64
          example: 1
      - name: createdFrom
        in: query
        description: "Created from (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-01T00:00:00
      - name: createdTo
        in: query
        description: "Created to (inclusive), ISO-8601."
        schema:
          type: string
          format: date-time
          example: 2026-05-31T23:59:59
      - name: page
        in: query
        description: Zero-based page index.
        schema:
          type: integer
          default: 0
      - name: size
        in: query
        description: Page size (max 100).
        schema:
          type: integer
          default: 20
      responses:
        "200":
          description: Paged list of file assets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/management/{id}:
    get:
      tags:
      - File management
      summary: Get file asset by id (admin)
      description: Returns the full catalog record for a single file asset. Does not
        return the file bytes; use the download-url endpoint to obtain a pre-signed
        S3 link. **ROLE_ADMIN** only.
      operationId: getById
      parameters:
      - name: id
        in: path
        description: File asset UUID.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: File asset.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileAssetResponse"
        "404":
          description: Asset not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
    delete:
      tags:
      - File management
      summary: Delete file asset (admin)
      description: "Soft-deletes the catalog entry and removes the object from S3.\
        \ **ROLE_ADMIN** only. Rate limit: **SENSITIVE_OPERATIONS**."
      operationId: delete
      parameters:
      - name: id
        in: path
        description: File asset UUID.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Deleted.
        "404":
          description: Asset not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/files/management/{id}/download-url:
    get:
      tags:
      - File management
      summary: Generate download URL (admin)
      description: Returns a 24-hour pre-signed S3 URL for the given asset. **ROLE_ADMIN**
        only.
      operationId: downloadUrl_1
      parameters:
      - name: id
        in: path
        description: File asset UUID.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Pre-signed download URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDownloadUrlResponse"
        "404":
          description: Asset not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/{employeeId}/attendances:
    get:
      tags:
      - HR — Employee attendance
      summary: List attendances by employee
      description: Paginated history for one employee. Optional `workDateFrom` / `workDateTo`
        (inclusive) filter by calendar work date. **404** if the employee does not
        exist.
      operationId: listAttendancesByEmployee
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: workDateFrom
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: workDateTo
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      responses:
        "200":
          description: Paged list (may be empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "404":
          description: Employee not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/summary:
    get:
      tags:
      - HR — Employees
      summary: Employee summary
      description: "High-level summary for dashboards. Rate limit: **READ_HEAVY**."
      operationId: getSummary
      responses:
        "200":
          description: Summary payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeSummaryResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/statistics:
    get:
      tags:
      - HR — Employees
      summary: Employee statistics
      description: "Aggregated counts and metrics for the workforce. Rate limit: **READ_HEAVY**."
      operationId: getStatistics_1
      responses:
        "200":
          description: Statistics payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeStatisticsResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/export:
    get:
      tags:
      - HR — Employees
      summary: Export employees to Excel
      description: "Streams an `.xlsx` report using the same filters as search (`status`,\
        \ `department`, `q`) and pagination (`page`, `size`). `Content-Disposition`\
        \ suggests a filename. Rate limit: **READ_HEAVY**."
      operationId: exportEmployees
      parameters:
      - name: status
        in: query
        description: Filtro por estado.
        required: false
        schema:
          type: string
          enum:
          - DRAFT
          - PENDING_CONTRACT
          - ACTIVE
          - SICK
          - ON_VACATION
          - ON_LEAVE
          - TERMINATED
          - FIRED
          - RESIGNED
          - UNDEFINED
      - name: department
        in: query
        description: Filtro por departamento.
        required: false
        schema:
          type: string
      - name: q
        in: query
        description: Búsqueda de texto.
        required: false
        schema:
          type: string
      - name: page
        in: query
        description: Índice de página (base 0).
        required: false
        schema:
          type: integer
          default: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (1–100).
        required: false
        schema:
          type: integer
          default: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Excel workbook bytes.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/attendances/{attendanceId}:
    get:
      tags:
      - HR — Employee attendance
      summary: Get attendance by id
      description: '**404** if not found (`ATTENDANCE_NOT_FOUND`).'
      operationId: getAttendanceById
      parameters:
      - name: attendanceId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: Attendance row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttendanceResponse"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/attendances/search:
    get:
      tags:
      - HR — Employee attendance
      summary: Search attendances
      description: Paginated search using optional filters; `onlyOpen=true` restricts
        to open check-ins.
      operationId: searchAttendances
      parameters:
      - name: employeeId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: headquarterId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: workDate
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: workDateFrom
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: workDateTo
        in: query
        required: false
        schema:
          type: string
          format: date
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - UNDEFINED
          - CHECKED_IN
          - CHECKED_OUT
          - AUTO_CLOSED_EXCEEDED_MAX_SHIFT_HOURS
          - AUTO_CLOSED_ASSUMED_CONTRACT_DAY
      - name: onlyOpen
        in: query
        required: false
        schema:
          type: boolean
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      responses:
        "200":
          description: Paged result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/attendances/for-today:
    get:
      tags:
      - HR — Employee attendance
      summary: List attendances for today
      description: Paginated attendances for the current calendar work date. Omit
        `headquarterId` to include all headquarters; pass it to restrict to one site.
      operationId: listAttendancesForToday
      parameters:
      - name: headquarterId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: pageable
        in: query
        required: true
        schema:
          $ref: "#/components/schemas/PageableRequest"
      responses:
        "200":
          description: Paged list (may be empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
  /api/v1/employees/active:
    get:
      tags:
      - HR — Employees
      summary: List active employees
      description: "Returns only **active** employments, paginated. Rate limit: **READ_HEAVY**."
      operationId: getActiveEmployees
      parameters:
      - name: page
        in: query
        description: Índice de página basado en cero.
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        example: 0
      - name: size
        in: query
        description: Tamaño de página (máximo 100).
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        example: 20
      responses:
        "200":
          description: Paged employee rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedResponse"
        "400":
          description: "Bad request: validation, malformed JSON, or constraint violation.\
            \ See `errorCode` (e.g. VALIDATION_FAILED, CONSTRAINT_VIOLATION)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "401":
          description: "Unauthorized: missing/invalid Bearer token, bad credentials,\
            \ or expired access token."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "403":
          description: "Forbidden: authenticated but not allowed for this resource."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "429":
          description: Too many requests (rate limit). Check `Retry-After` and `X-RateLimit-*`
            response headers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorResponse"
      security:
      - bearer-jwt: []
components:
  schemas:
    StorageLocationUpdateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        type:
          type: string
          enum:
          - WAREHOUSE
          - ZONE
          - AISLE
          - SHELF
          - BIN
        parentId:
          type: integer
          format: int64
        maxCapacity:
          type: integer
          format: int32
          minimum: 0
        occupiedCapacity:
          type: integer
          format: int32
          minimum: 0
        status:
          type: string
          enum:
          - ACTIVE
          - FULL
          - BLOCKED
          - INACTIVE
      required:
      - code
      - name
      - status
      - type
    StorageLocationResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        code:
          type: string
        name:
          type: string
        description:
          type: string
        type:
          type: string
          enum:
          - WAREHOUSE
          - ZONE
          - AISLE
          - SHELF
          - BIN
        parentId:
          type: integer
          format: int64
        maxCapacity:
          type: integer
          format: int32
        occupiedCapacity:
          type: integer
          format: int32
        status:
          type: string
          enum:
          - ACTIVE
          - FULL
          - BLOCKED
          - INACTIVE
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    ApiErrorResponse:
      type: object
      properties:
        errorCode:
          type: string
        message:
          type: string
        traceId:
          type: string
        context:
          type: object
          additionalProperties: {}
        fieldErrors:
          type: array
          items:
            $ref: "#/components/schemas/FieldError"
    FieldError:
      type: object
      properties:
        field:
          type: string
        message:
          type: string
    ItemUpdateRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        costPrice:
          type: number
        salePrice:
          type: number
        category:
          type: string
          enum:
          - RAW_MATERIAL
          - FINISHED_GOOD
          - CONSUMABLE
          - SPARE_PART
          - PACKAGING
          - TOOL
          - MACHINE
          - FURNITURE
          - OTHER
        unit:
          type: string
          enum:
          - PIECE
          - KG
          - GRAM
          - LITER
          - ML
          - BOX
          - DOZEN
          - METER
          - SQUARE_METER
        reorderPoint:
          type: integer
          format: int32
          minimum: 0
        reorderQuantity:
          type: integer
          format: int32
          minimum: 0
        brand:
          type: string
        barcode:
          type: string
        status:
          type: string
          enum:
          - ACTIVE
          - DISCONTINUED
          - OUT_OF_STOCK
          - PENDING_APPROVAL
      required:
      - category
      - costPrice
      - name
      - salePrice
      - sku
      - status
      - unit
    ItemResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        sku:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
          enum:
          - RAW_MATERIAL
          - FINISHED_GOOD
          - CONSUMABLE
          - SPARE_PART
          - PACKAGING
          - TOOL
          - MACHINE
          - FURNITURE
          - OTHER
        unit:
          type: string
          enum:
          - PIECE
          - KG
          - GRAM
          - LITER
          - ML
          - BOX
          - DOZEN
          - METER
          - SQUARE_METER
        brand:
          type: string
        barcode:
          type: string
        costPrice:
          type: number
        salePrice:
          type: number
        reorderPoint:
          type: integer
          format: int32
        reorderQuantity:
          type: integer
          format: int32
        status:
          type: string
          enum:
          - ACTIVE
          - DISCONTINUED
          - OUT_OF_STOCK
          - PENDING_APPROVAL
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    HeadQuarterRequest:
      type: object
      description: JSON body for create or update. **address** and **description**
        are optional (null or empty strings are normalized in the application/domain
        layers).
      properties:
        name:
          type: string
          description: Display name or unique key for the site.
          example: North Plant — Monterrey
          minLength: 1
        address:
          type: string
          description: Street or location (optional).
          example: "1200 Industrial Ave, Industrial Park"
        description:
          type: string
          description: Extended notes (optional).
          example: Raw materials receiving; 06:00–22:00.
      required:
      - name
    HeadQuarterResponse:
      type: object
      description: Headquarter representation. Text fields may be empty strings when
        absent in storage; **deletedAt** indicates soft delete.
      properties:
        id:
          type: integer
          format: int64
          description: Persisted id.
          example: 1
        name:
          type: string
          description: Site name.
          example: North Plant — Monterrey
        address:
          type: string
          description: Address.
          example: 1200 Industrial Ave
        description:
          type: string
          description: Description.
          example: Raw materials receiving.
        createdAt:
          type: string
          format: date-time
          description: Created at (audit).
        updatedAt:
          type: string
          format: date-time
          description: Last update (audit).
        deletedAt:
          type: string
          format: date-time
          description: Soft-delete timestamp; null when active.
        version:
          type: integer
          format: int64
          description: Optimistic lock version (JPA @Version).
          example: 3
    EmployeeUpdateMultipartBody:
      type: object
      description: "Send as **multipart/form-data**: required part **employee** =\
        \ JSON (schema **UpdateEmployeeRequest**), optional part **photo** = image\
        \ file."
      properties:
        employee:
          $ref: "#/components/schemas/UpdateEmployeeRequest"
          description: JSON payload for this part; prefer Content-Type **application/json**
            on the part. The server also accepts UTF-8 JSON if the part is sent as
            generic binary.
        photo:
          type: string
          format: binary
          description: Optional profile photo (e.g. JPEG or PNG).
      required:
      - employee
    UpdateEmployeeRequest:
      type: object
      properties:
        firstName:
          type: string
          minLength: 1
        lastName:
          type: string
          minLength: 1
        email:
          type: string
          format: email
          minLength: 1
        phone:
          type: string
          minLength: 1
        address:
          type: string
          minLength: 1
        curp:
          type: string
          minLength: 1
        rfc:
          type: string
          minLength: 1
        nss:
          type: string
          minLength: 1
        clabe:
          type: string
          minLength: 1
        position:
          type: string
          minLength: 1
        department:
          type: string
          minLength: 1
        contractType:
          type: string
          enum:
          - INDEFINITE
          - FIXED_TERM
          - PROJECT_BASED
          - TEMPORARY
          - FREELANCE
          - UNDEFINED
        workShift:
          type: string
          enum:
          - MORNING
          - AFTERNOON
          - NIGHT
          - MIXED
          - REMOTE
          - UNDEFINED
        salaryPerWeek:
          type: number
        bonuses:
          type: number
        foodVouchers:
          type: number
        integrationFactor:
          type: number
      required:
      - address
      - bonuses
      - clabe
      - contractType
      - curp
      - department
      - email
      - firstName
      - foodVouchers
      - integrationFactor
      - lastName
      - nss
      - phone
      - position
      - rfc
      - salaryPerWeek
      - workShift
    EmployeeResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        photoUrl:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        birthDate:
          type: string
          format: date
        nationality:
          type: string
        curp:
          type: string
        rfc:
          type: string
        nss:
          type: string
        clabe:
          type: string
        employeeNumber:
          type: string
        position:
          type: string
        department:
          type: string
        contractType:
          type: string
          enum:
          - INDEFINITE
          - FIXED_TERM
          - PROJECT_BASED
          - TEMPORARY
          - FREELANCE
          - UNDEFINED
        workShift:
          type: string
          enum:
          - MORNING
          - AFTERNOON
          - NIGHT
          - MIXED
          - REMOTE
          - UNDEFINED
        hireDate:
          type: string
          format: date
        terminationDate:
          type: string
          format: date
        status:
          type: string
          enum:
          - DRAFT
          - PENDING_CONTRACT
          - ACTIVE
          - SICK
          - ON_VACATION
          - ON_LEAVE
          - TERMINATED
          - FIRED
          - RESIGNED
          - UNDEFINED
        salaryPerWeek:
          type: number
        bonuses:
          type: number
        foodVouchers:
          type: number
        integrationFactor:
          type: number
        imssWorkerType:
          type: string
          enum:
          - PERMANENT_URBAN
          - EVENTUAL_URBAN
          - PERMANENT_RURAL
          - EVENTUAL_RURAL
        imssSalaryType:
          type: string
          enum:
          - FIXED
          - VARIABLE
          - MIXED
        christmasBonusDays:
          type: integer
          format: int32
        vacationDays:
          type: integer
          format: int32
        vacationPremiumPercent:
          type: number
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ReplaceEmployeeWorkScheduleRequest:
      type: object
      description: Full replacement of an employee weekly work schedule.
      properties:
        slots:
          type: array
          description: Ordered list of weekly slots; may be empty to clear the schedule.
          items:
            $ref: "#/components/schemas/WorkDayScheduleSlotRequest"
      required:
      - slots
    WorkDayScheduleSlotRequest:
      type: object
      description: Recurring weekly time window.
      properties:
        dayOfWeek:
          type: string
          description: Day of week for this window.
          enum:
          - MONDAY
          - TUESDAY
          - WEDNESDAY
          - THURSDAY
          - FRIDAY
          - SATURDAY
          - SUNDAY
          example: MONDAY
        startTime:
          type: string
          format: time
          description: Inclusive shift start (local time).
          example: 09:00
        endTime:
          type: string
          format: time
          description: Exclusive or inclusive end depending on client convention;
            must be after startTime.
          example: 18:00
      required:
      - dayOfWeek
      - endTime
      - startTime
    EmployeeWorkScheduleResponse:
      type: object
      description: Weekly work schedule slots for an employee.
      properties:
        slots:
          type: array
          description: Weekly slots; empty when no schedule is configured.
          items:
            $ref: "#/components/schemas/WorkDayScheduleSlotResponse"
    WorkDayScheduleSlotResponse:
      type: object
      properties:
        dayOfWeek:
          type: string
          enum:
          - MONDAY
          - TUESDAY
          - WEDNESDAY
          - THURSDAY
          - FRIDAY
          - SATURDAY
          - SUNDAY
          example: MONDAY
        startTime:
          type: string
          format: time
          example: 09:00
        endTime:
          type: string
          format: time
          example: 18:00
    UpdateContractRequest:
      type: object
      description: "Misma forma y reglas que el alta (**CreateContractRequest**):\
        \ actualización completa del estado expuesto por la API. Requiere `Authorization:\
        \ Bearer <token>`."
      properties:
        name:
          type: string
          description: Nombre o título del contrato.
          example: Contrato suministro materias primas 2025 (enmienda 1)
          minLength: 1
        description:
          type: string
          description: Descripción libre; puede omitirse.
          example: Acuerdo marco con proveedor homologado — anexo logística.
        category:
          type: string
          description: "Tipo de contrato: EMPLOYEE, SUPPLIER, CUSTOMER, PARTNER, OTHER."
          enum:
          - EMPLOYEE
          - SUPPLIER
          - CUSTOMER
          - PARTNER
          - OTHER
          - UNDEFINED
          example: SUPPLIER
        employeeId:
          type: integer
          format: int64
          description: Empleado vinculado; obligatorio si `category` es EMPLOYEE.
          example: 101
        opportunityId:
          type: integer
          format: int64
          description: Oportunidad CRM (opcional).
          example: 5001
        projectId:
          type: integer
          format: int64
          description: Proyecto (opcional).
          example: 88
        termKind:
          type: string
          description: "Plazo: FIXED_TERM o INDEFINITE."
          enum:
          - FIXED_TERM
          - INDEFINITE
          - UNDEFINED
          example: FIXED_TERM
        effectiveStart:
          type: string
          format: date
          description: Inicio de vigencia (ISO-8601 date).
          example: 2025-05-01
        effectiveEnd:
          type: string
          format: date
          description: Fin de vigencia (FIXED_TERM); omitir en INDEFINITE.
          example: 2026-10-31
        documentUrl:
          type: string
          description: URL del documento firmado o archivo archivado.
          example: https://storage.example.com/contracts/sup-2025-001-v2.pdf
          minLength: 1
        termsAndConditions:
          type: string
          description: Términos y condiciones (opcional).
          example: Pago a 45 días a partir de recepción de factura.
        referenceCode:
          type: string
          description: Código de referencia (opcional).
          example: CT-SUP-2025-042-R1
        renewalCycleMonths:
          type: integer
          format: int32
          description: Meses entre renovaciones automáticas (FIXED_TERM).
          example: 12
          minimum: 1
        agreedValue:
          type: number
          description: Valor pactado (opcional).
          example: 275000.0
          minimum: 0
        currencyCode:
          type: string
          description: Moneda ISO 4217 si hay valor pactado.
          example: MXN
          maxLength: 3
          minLength: 3
      required:
      - category
      - documentUrl
      - effectiveStart
      - name
      - termKind
    ContractResponse:
      type: object
      description: Representación de un contrato de negocio. Los textos opcionales
        nunca son `null` en JSON (cadena vacía). `category` o `termKind` pueden mostrar
        **UNDEFINED** si faltan en almacenamiento legado.
      properties:
        id:
          type: integer
          format: int64
          description: Identificador persistido; 0 o ausente antes de guardar en borradores.
          example: 42
        name:
          type: string
          description: Nombre o título del contrato.
          example: Contrato suministro 2025
        description:
          type: string
          description: Descripción.
          example: Acuerdo marco con proveedor homologado.
        category:
          type: string
          description: "Categoría: EMPLOYEE, SUPPLIER, CUSTOMER, PARTNER, OTHER, o\
            \ UNDEFINED si no hay dato."
          enum:
          - EMPLOYEE
          - SUPPLIER
          - CUSTOMER
          - PARTNER
          - OTHER
          - UNDEFINED
          example: SUPPLIER
        employeeId:
          type: integer
          format: int64
          description: Empleado vinculado (solo EMPLOYEE).
          example: 101
        opportunityId:
          type: integer
          format: int64
          description: Oportunidad CRM.
          example: 5001
        projectId:
          type: integer
          format: int64
          description: Proyecto.
          example: 88
        termKind:
          type: string
          description: "Tipo de plazo: FIXED_TERM, INDEFINITE, o UNDEFINED si no hay\
            \ dato."
          enum:
          - FIXED_TERM
          - INDEFINITE
          - UNDEFINED
          example: FIXED_TERM
        effectiveStart:
          type: string
          format: date
          description: Inicio de vigencia.
          example: 2025-05-01
        effectiveEnd:
          type: string
          format: date
          description: Fin de vigencia (null si indefinido o sin fin en modelo).
          example: 2026-04-30
        documentUrl:
          type: string
          description: URL del documento firmado o archivo.
          example: https://storage.example.com/contracts/sup-2025-001.pdf
        termsAndConditions:
          type: string
          description: Términos y condiciones.
          example: Pago a 30 días.
        referenceCode:
          type: string
          description: Código de referencia.
          example: CT-SUP-2025-042
        agreedValue:
          type: number
          description: Valor pactado (puede ser null).
          example: 250000.0
        currencyCode:
          type: string
          description: Moneda ISO 4217; cadena vacía si no aplica.
          example: MXN
        renewalCycleMonths:
          type: integer
          format: int32
          description: Meses de ciclo para renovación automática.
          example: 12
          minimum: 1
        extensionCount:
          type: integer
          format: int32
          description: Número de renovaciones o prórrogas registradas.
          example: 2
          minimum: 0
        lastRenewedAt:
          type: string
          format: date-time
          description: Marca de tiempo de la última renovación o extensión.
          example: 2026-01-15T10:30:00
        createdAt:
          type: string
          format: date-time
          description: Creación (audit).
        updatedAt:
          type: string
          format: date-time
          description: Última actualización (audit).
    ExtendContractRequest:
      type: object
      description: "Solicitud de prórroga: la nueva fecha de fin debe ser **estrictamente\
        \ posterior** al `effectiveEnd` actual. Solo aplica a contratos **FIXED_TERM**."
      properties:
        newEnd:
          type: string
          format: date
          description: "Nueva fecha de fin de vigencia (ISO-8601 date), después del\
            \ fin actual."
          example: 2027-12-31
      required:
      - newEnd
    AddRolesRequest:
      type: object
      properties:
        roles:
          type: array
          items:
            type: string
          minItems: 1
      required:
      - roles
    UserResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        email:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        gender:
          type: string
          enum:
          - MALE
          - FEMALE
          - NON_BINARY
          - OTHER
          - PREFER_NOT_TO_SAY
        phone:
          type: string
        dateOfBirth:
          type: string
          format: date
        accountStatus:
          type: string
          enum:
          - PENDING_APPROVAL
          - ACTIVE
          - BANNED
        bannedReason:
          type: string
        bannedAt:
          type: string
          format: date-time
        roles:
          type: array
          items:
            type: string
        permissions:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    BanUserRequest:
      type: object
      properties:
        reason:
          type: string
          maxLength: 500
          minLength: 0
    ChecklistLineRequest:
      type: object
      description: One checklist line on create.
      properties:
        description:
          type: string
          description: Line text.
          example: Order samples
          minLength: 1
        displayOrder:
          type: integer
          format: int32
          description: Stable order key for toggle API.
          example: 0
          minimum: 0
      required:
      - description
    TaskRequest:
      type: object
      description: Create-task payload. **title** is required. **priority** defaults
        to MEDIUM in the API layer when omitted. Format rules use Jakarta Validation.
      properties:
        title:
          type: string
          description: Task title.
          example: Follow up with supplier
          minLength: 1
        description:
          type: string
          description: Longer description (optional).
          example: Call before 17:00.
        priority:
          type: string
          description: Priority; omit for MEDIUM.
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - URGENT
          - UNDEFINED
          example: HIGH
        dueDate:
          type: string
          format: date-time
          description: Optional due date-time.
        headquarterId:
          type: integer
          format: int64
          description: Headquarter scope (optional).
          example: 1
        projectId:
          type: integer
          format: int64
          description: Project scope (optional).
          example: 10
        opportunityId:
          type: integer
          format: int64
          description: CRM opportunity scope (optional).
          example: 500
        createdById:
          type: integer
          format: int64
          description: Actor who created the task (optional).
          example: 42
        checklist:
          type: array
          description: Optional checklist lines.
          items:
            $ref: "#/components/schemas/ChecklistLineRequest"
      required:
      - title
    ChecklistItemResponse:
      type: object
      description: Checklist line in task responses.
      properties:
        description:
          type: string
          example: Order samples
        completed:
          type: boolean
          example: false
        completedAt:
          type: string
          format: date-time
        displayOrder:
          type: integer
          format: int32
          example: 0
    TaskResponse:
      type: object
      description: Task detail for API responses. Status/priority may show **UNDEFINED**
        when missing in storage.
      properties:
        id:
          type: integer
          format: int64
          description: Persisted id.
          example: 101
        title:
          type: string
          description: Title.
          example: Call supplier
        description:
          type: string
          description: Description.
          example: Confirm delivery slot.
        status:
          type: string
          description: Workflow status.
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
          - DELAYED
          - ON_HOLD
          - FAILED
          - UNDEFINED
          example: IN_PROGRESS
        priority:
          type: string
          description: Priority.
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - URGENT
          - UNDEFINED
          example: HIGH
        assignedToId:
          type: integer
          format: int64
          description: Assignee employee id.
          example: 7
        assignedById:
          type: integer
          format: int64
          description: Assigning user id.
          example: 3
        createdById:
          type: integer
          format: int64
          description: Creator user id.
          example: 3
        assignedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        dueDate:
          type: string
          format: date-time
        headquarterId:
          type: integer
          format: int64
          example: 1
        projectId:
          type: integer
          format: int64
          example: 10
        opportunityId:
          type: integer
          format: int64
          example: 500
        checklist:
          type: array
          description: Checklist lines.
          items:
            $ref: "#/components/schemas/ChecklistItemResponse"
        progressPercent:
          type: number
          format: double
          description: Completion percentage from checklist.
          example: 50.0
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TaskBulkImportResponse:
      type: object
      description: Counters for updated/created/skipped rows plus per-row errors.
      properties:
        updated:
          type: integer
          format: int32
          example: 3
        created:
          type: integer
          format: int32
          example: 5
        skipped:
          type: integer
          format: int32
          example: 1
        errors:
          type: array
          description: Row-level errors.
          items:
            $ref: "#/components/schemas/TaskBulkImportRowResponse"
    TaskBulkImportRowResponse:
      type: object
      description: One import error row.
      properties:
        excelRowNumber:
          type: integer
          format: int32
          description: 1-based Excel row number.
          example: 12
        message:
          type: string
          description: Error message.
          example: "Estado no válido: X"
    CreateProjectRequest:
      type: object
      properties:
        projectCode:
          type: string
          minLength: 1
        projectName:
          type: string
          minLength: 1
        description:
          type: string
        clientId:
          type: integer
          format: int64
        originOpportunityId:
          type: integer
          format: int64
        type:
          type: string
          enum:
          - CONSULTING
          - SOFTWARE_DEVELOPMENT
          - IMPLEMENTATION
          - MAINTENANCE
          - TRAINING
          - RESEARCH
          - OTHER
        priority:
          type: string
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - CRITICAL
        projectManagerId:
          type: integer
          format: int64
        assignedSalesmanId:
          type: integer
          format: int64
        plannedStartDate:
          type: string
          format: date
        plannedEndDate:
          type: string
          format: date
        contractedValue:
          type: number
        estimatedCost:
          type: number
      required:
      - clientId
      - contractedValue
      - estimatedCost
      - priority
      - projectCode
      - projectName
      - type
    ProjectResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        clientId:
          type: integer
          format: int64
        originOpportunityId:
          type: integer
          format: int64
        projectCode:
          type: string
        projectName:
          type: string
        description:
          type: string
        type:
          type: string
          enum:
          - CONSULTING
          - SOFTWARE_DEVELOPMENT
          - IMPLEMENTATION
          - MAINTENANCE
          - TRAINING
          - RESEARCH
          - OTHER
        status:
          type: string
          enum:
          - PLANNING
          - ACTIVE
          - ON_HOLD
          - COMPLETED
          - CANCELLED
          - ARCHIVED
        priority:
          type: string
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - CRITICAL
        projectManagerId:
          type: integer
          format: int64
        assignedSalesmanId:
          type: integer
          format: int64
        plannedStartDate:
          type: string
          format: date
        plannedEndDate:
          type: string
          format: date
        actualStartDate:
          type: string
          format: date
        actualEndDate:
          type: string
          format: date
        onHoldReason:
          type: string
        contractedValue:
          type: number
        estimatedCost:
          type: number
        actualCost:
          type: number
        progressPercent:
          type: integer
          format: int32
        cancellationReason:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateProjectMilestoneRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        plannedDate:
          type: string
          format: date
        billingAmount:
          type: number
        sortOrder:
          type: integer
          format: int32
      required:
      - billingAmount
      - name
    ProjectMilestoneResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        projectId:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
        status:
          type: string
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - DELAYED
          - CANCELLED
        plannedDate:
          type: string
          format: date
        actualDate:
          type: string
          format: date
        billingAmount:
          type: number
        billed:
          type: boolean
        sortOrder:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ReasonRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
      required:
      - reason
    RegisterPayrollRecordRequest:
      type: object
      description: Payload to create payroll record for an employee.
      properties:
        employeeId:
          type: integer
          format: int64
          description: Employee id.
          example: 101
        periodId:
          type: integer
          format: int64
          description: Optional payroll period id.
          example: 22
        workedDaysStart:
          type: string
          format: date
          description: Worked days range start.
          example: 2026-04-01
        workedDaysEnd:
          type: string
          format: date
          description: Worked days range end.
          example: 2026-04-07
        grossAmount:
          type: number
          description: Gross amount.
          example: 3500.0
      required:
      - employeeId
      - grossAmount
      - workedDaysEnd
      - workedDaysStart
    PayrollRecordResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        employeeId:
          type: integer
          format: int64
        periodId:
          type: integer
          format: int64
        workedDaysStart:
          type: string
          format: date
        workedDaysEnd:
          type: string
          format: date
        grossAmount:
          type: number
        totalDiscounts:
          type: number
        totalBonuses:
          type: number
        netAmount:
          type: number
        status:
          type: string
          enum:
          - PENDING
          - PAID
          - PARTIAL
          - DEFERRED
    RegisterPayrollAdjustmentRequest:
      type: object
      description: Discount or bonus to apply to a payroll record.
      properties:
        type:
          type: string
          description: Adjustment type.
          enum:
          - DISCOUNT
          - BONUS
          example: DISCOUNT
        amount:
          type: number
          description: Adjustment amount.
          example: 150.0
        reason:
          type: string
          description: Reason.
          example: Attendance bonus
          minLength: 1
      required:
      - amount
      - reason
      - type
    PayrollPaymentResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        employeeId:
          type: integer
          format: int64
        frequency:
          type: string
          enum:
          - WEEKLY
          - BIWEEKLY
          - MONTHLY
          - CUSTOM
        workedDaysStart:
          type: string
          format: date
        workedDaysEnd:
          type: string
          format: date
        grossAmount:
          type: number
        netAmount:
          type: number
        destinationAccount:
          type: string
        transactionId:
          type: string
        status:
          type: string
          enum:
          - PENDING
          - PAID
          - PARTIAL
          - DEFERRED
        pendingAmount:
          type: number
        createdAt:
          type: string
          format: date-time
    RegisterPayrollPeriodRequest:
      type: object
      description: Payload to create a payroll period.
      properties:
        frequency:
          type: string
          description: Payroll frequency.
          enum:
          - WEEKLY
          - BIWEEKLY
          - MONTHLY
          - CUSTOM
          example: WEEKLY
        startDate:
          type: string
          format: date
          description: Period start date.
          example: 2026-04-01
        endDate:
          type: string
          format: date
          description: Period end date.
          example: 2026-04-07
      required:
      - endDate
      - frequency
      - startDate
    PayrollPeriodResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        frequency:
          type: string
          enum:
          - WEEKLY
          - BIWEEKLY
          - MONTHLY
          - CUSTOM
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        status:
          type: string
    RegisterPayrollPaymentRequest:
      type: object
      description: Payload to register a payroll payment.
      properties:
        employeeId:
          type: integer
          format: int64
          description: Employee id.
          example: 101
        frequency:
          type: string
          description: Payroll frequency.
          enum:
          - WEEKLY
          - BIWEEKLY
          - MONTHLY
          - CUSTOM
          example: WEEKLY
        workedDaysStart:
          type: string
          format: date
          description: Worked days range start.
          example: 2026-04-01
        workedDaysEnd:
          type: string
          format: date
          description: Worked days range end.
          example: 2026-04-07
        grossAmount:
          type: number
          description: Gross amount.
          example: 3500.0
        netAmount:
          type: number
          description: Net amount sent to employee.
          example: 3350.0
        destinationAccount:
          type: string
          description: Destination account number/alias.
          example: "012180001234567890"
          minLength: 1
        transactionId:
          type: string
          description: Payment transaction id.
          example: TX-2026-000134
          minLength: 1
      required:
      - destinationAccount
      - employeeId
      - frequency
      - grossAmount
      - netAmount
      - transactionId
      - workedDaysEnd
      - workedDaysStart
    PayrollBulkScopeRequest:
      type: object
      description: Scope filters for payroll bulk import/export.
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        employeeId:
          type: integer
          format: int64
          description: Filter by employee id.
          example: 101
        periodId:
          type: integer
          format: int64
          description: Filter by payroll period id.
          example: 22
        from:
          type: string
          format: date
          description: Worked range start date (inclusive).
          example: 2026-04-01
        to:
          type: string
          format: date
          description: Worked range end date (inclusive).
          example: 2026-04-30
    SpreadsheetBulkImportResult:
      type: object
      description: "Resultado de importación masiva: contadores de filas actualizadas,\
        \ creadas, omitidas y lista de errores por fila."
      properties:
        updated:
          type: integer
          format: int32
          description: Filas actualizadas (match por id u otra regla del módulo).
          example: 5
        created:
          type: integer
          format: int32
          description: Filas insertadas como nuevos registros.
          example: 12
        skipped:
          type: integer
          format: int32
          description: "Filas ignoradas (sin efecto, p. ej. sin datos mínimos)."
          example: 1
        errors:
          type: array
          description: Errores por fila; vacío si todo OK.
          items:
            $ref: "#/components/schemas/SpreadsheetBulkImportRowError"
    SpreadsheetBulkImportRowError:
      type: object
      description: Detalle de error por fila del Excel (número de fila y mensaje legible).
      properties:
        excelRowNumber:
          type: integer
          format: int32
          description: "Número de fila en la hoja (1-based, típicamente como en Excel)."
          example: 7
        message:
          type: string
          description: Motivo del fallo.
          example: El nombre es obligatorio
    CreateOpportunityRequest:
      type: object
      description: Payload to open a new sales opportunity (Jakarta format validation).
      properties:
        title:
          type: string
          description: Deal title.
          example: Enterprise rollout
          minLength: 1
        description:
          type: string
          description: Longer description.
          example: Phased deployment over 6 months.
        contactName:
          type: string
          description: Primary contact name.
          minLength: 1
        contactEmail:
          type: string
          description: Contact email.
          minLength: 1
        contactPhone:
          type: string
          description: Contact phone.
          example: +52-55-1234-5678
        companyName:
          type: string
          description: Company / account name.
          minLength: 1
        companyLocation:
          type: string
          description: City / region hint.
          example: "Monterrey, NL"
        industry:
          type: string
          description: Industry segment.
          example: Food & beverage
        estimatedValue:
          type: number
          description: Estimated deal value.
        probabilityPercent:
          type: integer
          format: int32
          description: Win probability 0–100.
          example: 25
          maximum: 100
          minimum: 0
        source:
          type: string
          description: Lead source.
          enum:
          - INBOUND
          - OUTBOUND
          - REFERRAL
          - SOCIAL_MEDIA
          - EVENT
          - COLD_CALL
          - OTHER
        expectedCloseDate:
          type: string
          format: date
          description: Target close date.
        assignedSalesmanId:
          type: integer
          format: int64
          description: Assigned salesperson (employee id).
          example: 3
      required:
      - companyName
      - contactEmail
      - contactName
      - estimatedValue
      - source
      - title
    OpportunityResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        contactName:
          type: string
        contactEmail:
          type: string
        contactPhone:
          type: string
        companyName:
          type: string
        companyLocation:
          type: string
        industry:
          type: string
        title:
          type: string
        description:
          type: string
        estimatedValue:
          type: number
        probabilityPercent:
          type: integer
          format: int32
        source:
          type: string
          enum:
          - INBOUND
          - OUTBOUND
          - REFERRAL
          - SOCIAL_MEDIA
          - EVENT
          - COLD_CALL
          - OTHER
        status:
          type: string
          enum:
          - NEW
          - DISCOVERY
          - PROPOSAL
          - NEGOTIATION
          - WON
          - LOST
          - ABANDONED
        expectedCloseDate:
          type: string
          format: date
        actualCloseDate:
          type: string
          format: date
        assignedSalesmanId:
          type: integer
          format: int64
        lostReason:
          type: string
        convertedProjectId:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    WinOpportunityRequest:
      type: object
      properties:
        projectCode:
          type: string
          minLength: 1
        projectName:
          type: string
          minLength: 1
        description:
          type: string
        clientId:
          type: integer
          format: int64
        type:
          type: string
          enum:
          - CONSULTING
          - SOFTWARE_DEVELOPMENT
          - IMPLEMENTATION
          - MAINTENANCE
          - TRAINING
          - RESEARCH
          - OTHER
        priority:
          type: string
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - CRITICAL
        projectManagerId:
          type: integer
          format: int64
        assignedSalesmanId:
          type: integer
          format: int64
        plannedStartDate:
          type: string
          format: date
        plannedEndDate:
          type: string
          format: date
        contractedValue:
          type: number
        estimatedCost:
          type: number
      required:
      - clientId
      - contractedValue
      - estimatedCost
      - priority
      - projectCode
      - projectName
      - type
    LoseOpportunityRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
      required:
      - reason
    InventoryMovementResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        itemId:
          type: integer
          format: int64
        itemSku:
          type: string
        sourceLocationId:
          type: integer
          format: int64
        sourceLocationCode:
          type: string
        destinationLocationId:
          type: integer
          format: int64
        destinationLocationCode:
          type: string
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
        type:
          type: string
          enum:
          - PURCHASE
          - RETURN_FROM_CLIENT
          - INITIAL_STOCK
          - SALE
          - RETURN_TO_SUPPLIER
          - USAGE
          - SCRAP
          - TRANSFER
          - ADJUSTMENT_PLUS
          - ADJUSTMENT_MINUS
        direction:
          type: string
          enum:
          - IN
          - OUT
          - NEUTRAL
        description:
          type: string
        referenceNumber:
          type: string
        performedById:
          type: integer
          format: int64
        stockAfterMovement:
          type: integer
          format: int32
        transactionId:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    InventoryTransactionResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        transactionNumber:
          type: string
        type:
          type: string
          enum:
          - PURCHASE_RECEIPT
          - SALE_DISPATCH
          - INTERNAL_TRANSFER
          - PHYSICAL_COUNT
          - RETURN_FROM_CLIENT
          - RETURN_TO_SUPPLIER
          - PRODUCTION_ISSUE
          - SCRAP_WRITE_OFF
        status:
          type: string
          enum:
          - DRAFT
          - PENDING
          - APPROVED
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        approvedById:
          type: integer
          format: int64
        approvedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
        movements:
          type: array
          items:
            $ref: "#/components/schemas/InventoryMovementResponse"
    ApproveTransactionRequest:
      type: object
      properties:
        approvedById:
          type: integer
          format: int64
      required:
      - approvedById
    TransferLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        fromLocationId:
          type: integer
          format: int64
        toLocationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    TransferTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/TransferLineRequest"
          minItems: 1
      required:
      - lines
    ScrapLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    ScrapTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/ScrapLineRequest"
          minItems: 1
      required:
      - lines
    SaleLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    SaleTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/SaleLineRequest"
          minItems: 1
      required:
      - lines
    ReturnSupplierLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    ReturnSupplierTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/ReturnSupplierLineRequest"
          minItems: 1
      required:
      - lines
    ReturnClientLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    ReturnClientTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/ReturnClientLineRequest"
          minItems: 1
      required:
      - lines
    PurchaseLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
        unitCost:
          type: number
      required:
      - unitCost
    PurchaseTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseLineRequest"
          minItems: 1
      required:
      - lines
    PhysicalAdjustmentTransactionRequest:
      type: object
      properties:
        inventoryId:
          type: integer
          format: int64
        newQuantity:
          type: integer
          format: int32
          minimum: 0
        reason:
          type: string
          minLength: 1
        performedById:
          type: integer
          format: int64
      required:
      - inventoryId
      - reason
    AdjustmentLineRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        newQuantity:
          type: integer
          format: int32
        reason:
          type: string
    AdjustmentTransactionRequest:
      type: object
      properties:
        externalReference:
          type: string
        notes:
          type: string
        initiatedById:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: "#/components/schemas/AdjustmentLineRequest"
          minItems: 1
      required:
      - lines
    CreateInitialStockRequest:
      type: object
      properties:
        itemId:
          type: integer
          format: int64
        locationId:
          type: integer
          format: int64
        initialQuantity:
          type: integer
          format: int32
          minimum: 0
      required:
      - itemId
      - locationId
    InventoryStockResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        itemId:
          type: integer
          format: int64
        itemSku:
          type: string
        itemName:
          type: string
        locationId:
          type: integer
          format: int64
        locationCode:
          type: string
        locationName:
          type: string
        availableQuantity:
          type: integer
          format: int32
        reservedQuantity:
          type: integer
          format: int32
        inTransitQuantity:
          type: integer
          format: int32
        status:
          type: string
          enum:
          - NORMAL
          - LOW_STOCK
          - OUT_OF_STOCK
          - OVERSTOCKED
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
    StorageLocationCreateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        type:
          type: string
          enum:
          - WAREHOUSE
          - ZONE
          - AISLE
          - SHELF
          - BIN
        parentId:
          type: integer
          format: int64
        maxCapacity:
          type: integer
          format: int32
          minimum: 0
      required:
      - code
      - name
      - type
    ItemCreateRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
        costPrice:
          type: number
        salePrice:
          type: number
        category:
          type: string
          enum:
          - RAW_MATERIAL
          - FINISHED_GOOD
          - CONSUMABLE
          - SPARE_PART
          - PACKAGING
          - TOOL
          - MACHINE
          - FURNITURE
          - OTHER
        unit:
          type: string
          enum:
          - PIECE
          - KG
          - GRAM
          - LITER
          - ML
          - BOX
          - DOZEN
          - METER
          - SQUARE_METER
        reorderPoint:
          type: integer
          format: int32
          minimum: 0
        reorderQuantity:
          type: integer
          format: int32
          minimum: 0
        brand:
          type: string
        barcode:
          type: string
      required:
      - category
      - costPrice
      - name
      - salePrice
      - sku
      - unit
    FileAssetResponse:
      type: object
      description: Stored company file asset.
      properties:
        id:
          type: string
          format: uuid
          description: Asset id.
          example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category:
          type: string
          description: Storage category.
          enum:
          - TEMPLATE
          - COMPANY
          - EXTRAS
          - RESOURCE
          example: TEMPLATE
        module:
          type: string
          description: Owning module (RESOURCE only).
          example: inventory
        entityType:
          type: string
          description: Entity type tag (RESOURCE only).
          example: inventory-item
        entityId:
          type: integer
          format: int64
          description: Entity id (RESOURCE only).
          example: 42
        s3Key:
          type: string
          description: S3 object key.
          example: pimienta/sources/templates/invoice_template_1716916800000_3742.xlsx
        originalName:
          type: string
          description: Original filename.
          example: invoice_template.xlsx
        contentType:
          type: string
          description: MIME type.
          example: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        fileSizeBytes:
          type: integer
          format: int64
          description: File size in bytes.
          example: 204800
        description:
          type: string
          description: Optional description.
        uploadedByUserId:
          type: integer
          format: int64
          description: User id who uploaded the file.
          example: 1
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp.
          example: 2026-05-28T10:15:00
    EmployeeRegisterMultipartBody:
      type: object
      description: "Send as **multipart/form-data**: required part **employee** =\
        \ JSON (schema **RegisterEmployeeRequest**), optional part **photo** = image\
        \ file."
      properties:
        employee:
          $ref: "#/components/schemas/RegisterEmployeeRequest"
          description: JSON payload for this part; prefer Content-Type **application/json**
            on the part. The server also accepts UTF-8 JSON if the part is sent as
            generic binary.
        photo:
          type: string
          format: binary
          description: Optional profile photo (e.g. JPEG or PNG).
      required:
      - employee
    RegisterEmployeeRequest:
      type: object
      description: Payload to register a new employee in onboarding (draft or pending
        employment contract).
      properties:
        firstName:
          type: string
          description: First name.
          example: María
          minLength: 1
        lastName:
          type: string
          description: Last name.
          example: López García
          minLength: 1
        email:
          type: string
          format: email
          description: Work email address.
          example: maria.lopez@empresa.example
          minLength: 1
        phone:
          type: string
          description: Contact phone number.
          example: +52 55 1234 5678
          minLength: 1
        address:
          type: string
          description: Street address.
          example: "Av. Reforma 123, CDMX"
          minLength: 1
        curp:
          type: string
          description: CURP (18 characters).
          example: XXXX850101HDFXXX09
          maxLength: 18
          minLength: 18
        rfc:
          type: string
          description: RFC (12 or 13 characters).
          example: XAXX010101000
          maxLength: 13
          minLength: 12
        nss:
          type: string
          description: IMSS NSS (11 digits).
          example: "12345678901"
          minLength: 1
        clabe:
          type: string
          description: 18-digit CLABE for payroll deposits.
          example: "012180001234567890"
          minLength: 1
        employeeNumber:
          type: string
          description: Internal employee number.
          example: EMP-1042
          minLength: 1
        position:
          type: string
          description: Job title or position.
          example: Operador de línea
          minLength: 1
        department:
          type: string
          description: Department or area.
          example: Producción
          minLength: 1
        contractType:
          type: string
          description: Employment contract type.
          enum:
          - INDEFINITE
          - FIXED_TERM
          - PROJECT_BASED
          - TEMPORARY
          - FREELANCE
          - UNDEFINED
        workShift:
          type: string
          description: Work shift.
          enum:
          - MORNING
          - AFTERNOON
          - NIGHT
          - MIXED
          - REMOTE
          - UNDEFINED
        salaryPerWeek:
          type: number
          description: Gross weekly salary in MXN.
          example: 3500.0
          minimum: 0
        birthDate:
          type: string
          format: date
          description: Date of birth (ISO-8601 date).
          example: 1985-01-01
        onboardingPhase:
          type: string
          description: "Onboarding phase: DRAFT (still being captured) or PENDING_CONTRACT\
            \ (awaiting contract)."
          enum:
          - DRAFT
          - PENDING_CONTRACT
      required:
      - address
      - birthDate
      - clabe
      - contractType
      - curp
      - department
      - email
      - employeeNumber
      - firstName
      - lastName
      - nss
      - onboardingPhase
      - phone
      - position
      - rfc
      - salaryPerWeek
      - workShift
    AttendanceStartMultipartBody:
      type: object
      description: "Send as **multipart/form-data**: part **attendance** = JSON (**StartWorkdayRequest**),\
        \ optional part **checkInEvidencePhoto** = image file."
      properties:
        attendance:
          $ref: "#/components/schemas/StartWorkdayRequest"
          description: JSON payload; prefer Content-Type **application/json** on the
            part. The server also accepts UTF-8 JSON if the part is sent as generic
            binary.
        checkInEvidencePhoto:
          type: string
          format: binary
          description: Optional check-in evidence image (e.g. JPEG or PNG).
      required:
      - attendance
    StartWorkdayRequest:
      type: object
      description: "JSON part for check-in when using **multipart/form-data** (`attendance`\
        \ part), or full body for **application/json**. Evidence photos are uploaded\
        \ as the **`checkInEvidencePhoto`** multipart part only (not a URL field)."
      properties:
        headquarterId:
          type: integer
          format: int64
          description: Headquarter where the employee is clocking in.
          example: 3
        workDate:
          type: string
          format: date
          description: Optional calendar work date; defaults to today when omitted.
          example: 2026-05-14
      required:
      - headquarterId
    AttendanceResponse:
      type: object
      description: Single employee attendance record.
      properties:
        id:
          type: integer
          format: int64
          description: Primary key.
          example: 42
        employeeId:
          type: integer
          format: int64
          description: Employee id.
          example: 7
        headquarterId:
          type: integer
          format: int64
          description: Headquarter id.
          example: 3
        workDate:
          type: string
          format: date
          description: Calendar work date.
          example: 2026-05-14
        checkInTime:
          type: string
          format: date-time
          description: Check-in timestamp.
        checkOutTime:
          type: string
          format: date-time
          description: Check-out timestamp; null while still checked in.
        status:
          type: string
          description: Lifecycle status.
          enum:
          - UNDEFINED
          - CHECKED_IN
          - CHECKED_OUT
          - AUTO_CLOSED_EXCEEDED_MAX_SHIFT_HOURS
          - AUTO_CLOSED_ASSUMED_CONTRACT_DAY
        checkInEvidencePhotoUrl:
          type: string
          description: "Check-in evidence: **HTTPS presigned URL** when stored value\
            \ is an S3 object key; legacy rows may return a plain URL. Empty when\
            \ no photo was uploaded."
        checkOutEvidencePhotoUrl:
          type: string
          description: "Check-out evidence: **HTTPS presigned URL** when stored value\
            \ is an S3 object key; legacy rows may return a plain URL. Empty when\
            \ no photo was uploaded."
        minutesWorked:
          type: integer
          format: int64
          description: Minutes between check-in and check-out (0 if open).
          example: 480
    AttendanceEndMultipartBody:
      type: object
      description: "Send as **multipart/form-data**: part **attendance** = JSON (**EndWorkdayRequest**),\
        \ optional part **checkOutEvidencePhoto** = image file."
      properties:
        attendance:
          $ref: "#/components/schemas/EndWorkdayRequest"
          description: JSON payload; prefer Content-Type **application/json** on the
            part. The server also accepts UTF-8 JSON if the part is sent as generic
            binary.
        checkOutEvidencePhoto:
          type: string
          format: binary
          description: Optional check-out evidence image (e.g. JPEG or PNG).
      required:
      - attendance
    EndWorkdayRequest:
      type: object
      description: "JSON part for check-out when using **multipart/form-data** (`attendance`\
        \ part), or full body for **application/json**. Evidence photos are uploaded\
        \ as the **`checkOutEvidencePhoto`** multipart part only (not a URL field)."
      properties:
        workDate:
          type: string
          format: date
          description: Optional calendar work date; defaults to today when omitted.
          example: 2026-05-14
    CreateContractRequest:
      type: object
      description: "Cuerpo para registrar un contrato. **EMPLOYEE** exige `employeeId`.\
        \ **FIXED_TERM** exige `effectiveEnd` posterior a `effectiveStart`. **INDEFINITE**\
        \ no debe enviar `effectiveEnd`. Si se envía `agreedValue`, `currencyCode`\
        \ debe ser ISO 4217 de 3 letras (p. ej. MXN)."
      properties:
        name:
          type: string
          description: Nombre o título del contrato (visible en listados).
          example: Contrato suministro materias primas 2025
          minLength: 1
        description:
          type: string
          description: Descripción libre; puede omitirse (se interpreta como vacío).
          example: Acuerdo marco con proveedor homologado.
        category:
          type: string
          description: "Tipo de contrato según la contraparte: EMPLOYEE, SUPPLIER,\
            \ CUSTOMER, PARTNER, OTHER."
          enum:
          - EMPLOYEE
          - SUPPLIER
          - CUSTOMER
          - PARTNER
          - OTHER
          - UNDEFINED
          example: SUPPLIER
        employeeId:
          type: integer
          format: int64
          description: Identificador del empleado; **obligatorio** si `category` es
            EMPLOYEE; debe omitirse en otros tipos.
          example: 101
        opportunityId:
          type: integer
          format: int64
          description: Oportunidad CRM vinculada (opcional). Debe existir si se envía.
          example: 5001
        projectId:
          type: integer
          format: int64
          description: Proyecto vinculado (opcional). Debe existir si se envía.
          example: 88
        termKind:
          type: string
          description: "Plazo: FIXED_TERM (con fin) o INDEFINITE (sin fecha de té\
            rmino en sistema)."
          enum:
          - FIXED_TERM
          - INDEFINITE
          - UNDEFINED
          example: FIXED_TERM
        effectiveStart:
          type: string
          format: date
          description: Inicio de vigencia (ISO-8601 date).
          example: 2025-05-01
        effectiveEnd:
          type: string
          format: date
          description: Fin de vigencia obligatorio para FIXED_TERM; no debe enviarse
            para INDEFINITE.
          example: 2026-04-30
        documentUrl:
          type: string
          description: URL del documento firmado o archivo archivado (obligatorio
            en esta API).
          example: https://storage.example.com/contracts/sup-2025-001.pdf
          minLength: 1
        termsAndConditions:
          type: string
          description: Texto de términos y condiciones (opcional).
          example: Pago a 30 días; incumplimiento según legislación aplicable.
        referenceCode:
          type: string
          description: Código interno o folio de referencia (opcional).
          example: CT-SUP-2025-042
        renewalCycleMonths:
          type: integer
          format: int32
          description: Meses para renovación automática (`POST .../renew`); solo aplica
            a FIXED_TERM con ciclo definido.
          example: 12
          minimum: 1
        agreedValue:
          type: number
          description: "Valor pactado (opcional). Si se envía, usar también `currencyCode`."
          example: 250000.0
          minimum: 0
        currencyCode:
          type: string
          description: "Código de moneda ISO 4217 (3 letras), obligatorio si hay `agreedValue`."
          example: MXN
          maxLength: 3
          minLength: 3
      required:
      - category
      - documentUrl
      - effectiveStart
      - name
      - termKind
    RegisterRequest:
      type: object
      properties:
        firstName:
          type: string
          maxLength: 120
          minLength: 0
        lastName:
          type: string
          maxLength: 120
          minLength: 0
        gender:
          type: string
          enum:
          - MALE
          - FEMALE
          - NON_BINARY
          - OTHER
          - PREFER_NOT_TO_SAY
        email:
          type: string
          format: email
          minLength: 1
        phone:
          type: string
          maxLength: 32
          minLength: 8
          pattern: "^[+]?[0-9\\s().-]{8,31}$"
        password:
          type: string
          minLength: 1
        dateOfBirth:
          type: string
          format: date
      required:
      - dateOfBirth
      - email
      - firstName
      - gender
      - lastName
      - password
      - phone
    RegisterResponse:
      type: object
      properties:
        message:
          type: string
        status:
          type: string
    RefreshRequest:
      type: object
      properties:
        refreshToken:
          type: string
          minLength: 1
      required:
      - refreshToken
    TokenResponse:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        tokenType:
          type: string
        expiresInSeconds:
          type: integer
          format: int64
    LogoutRequest:
      type: object
      properties:
        refreshToken:
          type: string
    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          minLength: 1
        password:
          type: string
          minLength: 1
      required:
      - email
      - password
    UpdateProfileRequest:
      type: object
      properties:
        firstName:
          type: string
          maxLength: 120
          minLength: 0
        lastName:
          type: string
          maxLength: 120
          minLength: 0
        gender:
          type: string
          enum:
          - MALE
          - FEMALE
          - NON_BINARY
          - OTHER
          - PREFER_NOT_TO_SAY
        phone:
          type: string
          maxLength: 32
          minLength: 8
          pattern: "^[+]?[0-9\\s().-]{8,31}$"
        dateOfBirth:
          type: string
          format: date
      required:
      - dateOfBirth
      - firstName
      - gender
      - lastName
      - phone
    StatusUpdateRequest:
      type: object
      description: New status for PATCH status.
      properties:
        status:
          type: string
          description: Target workflow status.
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
          - DELAYED
          - ON_HOLD
          - FAILED
          - UNDEFINED
          example: COMPLETED
      required:
      - status
    AssignTaskRequest:
      type: object
      description: Assignee for PATCH assign.
      properties:
        employeeId:
          type: integer
          format: int64
          description: Employee id to assign the task to.
          example: 42
      required:
      - employeeId
    UpdateProjectMilestoneRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        plannedDate:
          type: string
          format: date
        billingAmount:
          type: number
        sortOrder:
          type: integer
          format: int32
    UpdateProjectRequest:
      type: object
      properties:
        projectName:
          type: string
        description:
          type: string
        type:
          type: string
          enum:
          - CONSULTING
          - SOFTWARE_DEVELOPMENT
          - IMPLEMENTATION
          - MAINTENANCE
          - TRAINING
          - RESEARCH
          - OTHER
        priority:
          type: string
          enum:
          - LOW
          - MEDIUM
          - HIGH
          - CRITICAL
        projectManagerId:
          type: integer
          format: int64
        assignedSalesmanId:
          type: integer
          format: int64
        plannedStartDate:
          type: string
          format: date
        plannedEndDate:
          type: string
          format: date
        contractedValue:
          type: number
        estimatedCost:
          type: number
        progressPercent:
          type: integer
          format: int32
    UpdateOpportunityRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        contactName:
          type: string
        contactEmail:
          type: string
        contactPhone:
          type: string
        companyName:
          type: string
        companyLocation:
          type: string
        industry:
          type: string
        estimatedValue:
          type: number
        probabilityPercent:
          type: integer
          format: int32
        source:
          type: string
          enum:
          - INBOUND
          - OUTBOUND
          - REFERRAL
          - SOCIAL_MEDIA
          - EVENT
          - COLD_CALL
          - OTHER
        expectedCloseDate:
          type: string
          format: date
        assignedSalesmanId:
          type: integer
          format: int64
    HealthResponse:
      type: object
      description: Public liveness probe for load balancers and orchestrators.
      properties:
        status:
          type: string
          description: Application health status.
          example: UP
        service:
          type: string
          description: Spring application name.
          example: pimienta
    UserDashboardResponse:
      type: object
      properties:
        totalActiveEmployees:
          type: integer
          format: int32
        totalActiveProjects:
          type: integer
          format: int32
        totalActiveHeadquarters:
          type: integer
          format: int32
        totalActivePersonalTasks:
          type: integer
          format: int32
        totalPendingPersonalTasks:
          type: integer
          format: int32
        totalActiveEmployeesTasks:
          type: integer
          format: int32
        totalEmployeePending:
          type: integer
          format: int32
    PageableRequest:
      type: object
      description: "Parámetros de paginación estándar (`page`, `size`)."
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
    PageMetadata:
      type: object
      description: "Metadatos de paginación (número de página, totales, banderas first/last)."
      properties:
        pageNumber:
          type: integer
          format: int32
          description: Índice de página actual (0-based).
          example: 0
          minimum: 0
        pageSize:
          type: integer
          format: int32
          description: Tamaño de página solicitado.
          example: 20
          minimum: 1
        totalElements:
          type: integer
          format: int64
          description: Total de elementos en todas las páginas.
          example: 150
          minimum: 0
        totalPages:
          type: integer
          format: int32
          description: Total de páginas.
          example: 8
          minimum: 0
        first:
          type: boolean
          description: Si es la primera página.
          example: true
        last:
          type: boolean
          description: Si es la última página.
          example: false
        hasNext:
          type: boolean
          description: Si existe página siguiente.
          example: true
        hasPrevious:
          type: boolean
          description: Si existe página anterior.
          example: false
    PagedResponse:
      type: object
      description: "Respuesta paginada genérica: lista `items` y objeto `metadata`\
        \ con totales y banderas de navegación. El esquema de cada elemento depende\
        \ del endpoint (p. ej. ContractResponse)."
      properties:
        items:
          type: array
          description: Elementos de la página actual.
          items: {}
        metadata:
          $ref: "#/components/schemas/PageMetadata"
          description: Metadatos de paginación.
    UserStatisticsResponse:
      type: object
      properties:
        totalUsers:
          type: integer
          format: int64
        activeUsers:
          type: integer
          format: int64
        bannedUsers:
          type: integer
          format: int64
    TaskSearchRequest:
      type: object
      description: Filters and pagination for GET /api/v1/tasks.
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        headquarterId:
          type: integer
          format: int64
          description: Filter by headquarter id.
          example: 1
        projectId:
          type: integer
          format: int64
          description: Filter by project id.
          example: 10
        opportunityId:
          type: integer
          format: int64
          description: Filter by CRM opportunity id.
          example: 500
        employeeId:
          type: integer
          format: int64
          description: Filter by assignee (employee) id.
          example: 7
        status:
          type: string
          description: Filter by status.
          enum:
          - PENDING
          - IN_PROGRESS
          - COMPLETED
          - CANCELLED
          - DELAYED
          - ON_HOLD
          - FAILED
          - UNDEFINED
          example: PENDING
    Pageable:
      type: object
      properties:
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          minimum: 1
        sort:
          type: array
          items:
            type: string
    ProjectSearchRequest:
      type: object
      description: "Parámetros de paginación estándar (`page`, `size`)."
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        status:
          type: string
          enum:
          - PLANNING
          - ACTIVE
          - ON_HOLD
          - COMPLETED
          - CANCELLED
          - ARCHIVED
        clientId:
          type: integer
          format: int64
        projectManagerId:
          type: integer
          format: int64
        originOpportunityId:
          type: integer
          format: int64
    ProjectSummaryResponse:
      type: object
      properties:
        project:
          $ref: "#/components/schemas/ProjectResponse"
        milestoneCount:
          type: integer
          format: int64
        milestoneCompletedCount:
          type: integer
          format: int64
        taskCount:
          type: integer
          format: int64
        overdue:
          type: boolean
    PayrollSummaryResponse:
      type: object
      properties:
        from:
          type: string
          format: date
        to:
          type: string
          format: date
        employeesPaid:
          type: integer
          format: int64
        totalGross:
          type: number
        totalNet:
          type: number
        totalDiscounts:
          type: number
        totalBonuses:
          type: number
        totalDebt:
          type: number
    PayrollRecordSearchRequest:
      type: object
      description: Filters for searching payroll records.
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        employeeId:
          type: integer
          format: int64
          description: Filter by employee id.
          example: 101
        periodId:
          type: integer
          format: int64
          description: Filter by payroll period id.
          example: 22
    OpportunitySearchRequest:
      type: object
      description: Query parameters for GET /api/v1/opportunities (extends pagination).
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        status:
          type: string
          description: Filter by pipeline status.
          enum:
          - NEW
          - DISCOVERY
          - PROPOSAL
          - NEGOTIATION
          - WON
          - LOST
          - ABANDONED
          example: PROPOSAL
        companyNameContains:
          type: string
          description: Substring match on company name (repository-defined semantics).
          example: Acme
        titleContains:
          type: string
          description: Substring match on title.
          example: rollout
    OpportunitySummaryResponse:
      type: object
      properties:
        opportunity:
          $ref: "#/components/schemas/OpportunityResponse"
        taskCount:
          type: integer
          format: int64
        openTaskCount:
          type: integer
          format: int64
        weightedValue:
          type: number
        overdue:
          type: boolean
    NotificationAdminSearchRequest:
      type: object
      description: Filters and pagination for admin notification search (GET /api/v1/notifications/management).
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        id:
          type: string
          format: uuid
          description: Filter by notification id.
        channel:
          type: string
          description: Filter by channel.
          enum:
          - EMAIL
          - SMS
          - LOG
          example: EMAIL
        type:
          type: string
          description: Filter by type.
          enum:
          - ACCOUNT_PENDING_APPROVAL
          - UNDEFINED
          example: ACCOUNT_PENDING_APPROVAL
        status:
          type: string
          description: Filter by status.
          enum:
          - PENDING
          - SENT
          - FAILED
          - SKIPPED
          example: SENT
        recipientUserId:
          type: integer
          format: int64
          description: Filter by recipient user id.
          example: 1
        relatedUserId:
          type: integer
          format: int64
          description: Filter by related user id.
          example: 42
        correlationId:
          type: string
          description: Correlation id contains (case-insensitive).
          example: account-pending
        recipientEmailContains:
          type: string
          description: Recipient email contains (case-insensitive).
          example: admin@
        recipientPhoneContains:
          type: string
          description: Recipient phone contains.
          example: "300"
        subjectContains:
          type: string
          description: Subject contains (case-insensitive).
          example: approval
        bodyContains:
          type: string
          description: Body contains (case-insensitive).
          example: registered
        createdFrom:
          type: string
          format: date-time
          description: "Created at from (inclusive), ISO-8601."
          example: 2026-05-01T00:00:00
        createdTo:
          type: string
          format: date-time
          description: "Created at to (inclusive), ISO-8601."
          example: 2026-05-28T23:59:59
        sentFrom:
          type: string
          format: date-time
          description: "Sent at from (inclusive), ISO-8601."
          example: 2026-05-01T00:00:00
        sentTo:
          type: string
          format: date-time
          description: "Sent at to (inclusive), ISO-8601."
          example: 2026-05-28T23:59:59
    NotificationStatisticsRequest:
      type: object
      description: Time interval for notification statistics (GET .../management/statistics).
      properties:
        from:
          type: string
          format: date-time
          description: "Interval start (inclusive), ISO-8601 date-time."
          example: 2026-05-01T00:00:00
        to:
          type: string
          format: date-time
          description: "Interval end (inclusive), ISO-8601 date-time."
          example: 2026-05-28T23:59:59
      required:
      - from
      - to
    NotificationStatisticsResponse:
      type: object
      description: Aggregated notification metrics for a time interval.
      properties:
        from:
          type: string
          format: date-time
          description: Interval start (inclusive).
          example: 2026-05-01T00:00:00
        to:
          type: string
          format: date-time
          description: Interval end (inclusive).
          example: 2026-05-28T23:59:59
        total:
          type: integer
          format: int64
          description: Total notifications in the interval.
          example: 120
        pending:
          type: integer
          format: int64
          description: Pending count.
          example: 3
        sent:
          type: integer
          format: int64
          description: Successfully sent count.
          example: 110
        failed:
          type: integer
          format: int64
          description: Failed count.
          example: 5
        skipped:
          type: integer
          format: int64
          description: Skipped count.
          example: 2
        countByChannel:
          type: object
          additionalProperties:
            type: integer
            format: int64
          description: Counts grouped by channel name.
          example:
            LOG: 80
            EMAIL: 30
        countByType:
          type: object
          additionalProperties:
            type: integer
            format: int64
          description: Counts grouped by type name.
          example:
            ACCOUNT_PENDING_APPROVAL: 120
        countByStatus:
          type: object
          additionalProperties:
            type: integer
            format: int64
          description: Counts grouped by status name.
          example:
            SENT: 110
            FAILED: 5
    NotificationManagerLogSearchRequest:
      type: object
      description: Filters for manager notification log search (GET /api/v1/notifications/logs).
        Only LOG channel entries from today are returned.
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        id:
          type: string
          format: uuid
          description: Filter by notification id.
        type:
          type: string
          description: Filter by type.
          enum:
          - ACCOUNT_PENDING_APPROVAL
          - UNDEFINED
          example: ACCOUNT_PENDING_APPROVAL
        status:
          type: string
          description: Filter by status.
          enum:
          - PENDING
          - SENT
          - FAILED
          - SKIPPED
          example: SENT
        recipientUserId:
          type: integer
          format: int64
          description: Filter by recipient user id.
          example: 1
        relatedUserId:
          type: integer
          format: int64
          description: Filter by related user id.
          example: 42
        correlationId:
          type: string
          description: Correlation id contains (case-insensitive).
          example: account-pending
        subjectContains:
          type: string
          description: Subject contains (case-insensitive).
          example: approval
        bodyContains:
          type: string
          description: Body contains (case-insensitive).
          example: registered
        sentFrom:
          type: string
          format: date-time
          description: "Sent at from (inclusive), within today, ISO-8601."
          example: 2026-05-28T08:00:00
        sentTo:
          type: string
          format: date-time
          description: "Sent at to (inclusive), within today, ISO-8601."
          example: 2026-05-28T18:00:00
    LocationTreeNodeResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        code:
          type: string
        name:
          type: string
        type:
          type: string
          enum:
          - WAREHOUSE
          - ZONE
          - AISLE
          - SHELF
          - BIN
        status:
          type: string
          enum:
          - ACTIVE
          - FULL
          - BLOCKED
          - INACTIVE
        parentId:
          type: integer
          format: int64
        maxCapacity:
          type: integer
          format: int32
        occupiedCapacity:
          type: integer
          format: int32
        children:
          type: array
          items:
            $ref: "#/components/schemas/LocationTreeNodeResponse"
    Page:
      type: object
      properties:
        totalPages:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int64
        first:
          type: boolean
        last:
          type: boolean
        size:
          type: integer
          format: int32
        content:
          type: array
          items: {}
        number:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        numberOfElements:
          type: integer
          format: int32
        empty:
          type: boolean
    PageableObject:
      type: object
      properties:
        offset:
          type: integer
          format: int64
        paged:
          type: boolean
        pageNumber:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
        unpaged:
          type: boolean
        sort:
          $ref: "#/components/schemas/SortObject"
    SortObject:
      type: object
      properties:
        empty:
          type: boolean
        sorted:
          type: boolean
        unsorted:
          type: boolean
    HeadquarterStatisticsResponse:
      type: object
      description: "Aggregated counts: total rows in the table, active (no `deletedAt`),\
        \ and soft-deleted."
      properties:
        total:
          type: integer
          format: int64
          description: Total rows including soft-deleted.
          example: 42
        active:
          type: integer
          format: int64
          description: Active sites (`deletedAt` is null).
          example: 40
        softDeleted:
          type: integer
          format: int64
          description: Soft-deleted sites.
          example: 2
    FileResourceSearchRequest:
      type: object
      description: Filters for GET /api/v1/files/resources (RESOURCE category only).
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        module:
          type: string
          description: Module slug to query resources for.
          example: inventory
          minLength: 1
        entityType:
          type: string
          description: Filter by entity type.
          example: inventory-item
        entityId:
          type: integer
          format: int64
          description: Filter by entity id.
          example: 42
        originalNameContains:
          type: string
          description: Original filename contains (case-insensitive).
          example: report
        contentTypeContains:
          type: string
          description: Content type contains.
          example: pdf
        createdFrom:
          type: string
          format: date-time
          description: "Created from (inclusive), ISO-8601."
          example: 2026-05-01T00:00:00
        createdTo:
          type: string
          format: date-time
          description: "Created to (inclusive), ISO-8601."
          example: 2026-05-31T23:59:59
      required:
      - module
    FileDownloadUrlResponse:
      type: object
      description: Pre-signed 24-hour download URL for a file asset.
      properties:
        id:
          type: string
          format: uuid
          description: Asset id.
        url:
          type: string
          description: Pre-signed S3 download URL (valid 24 h).
          example: https://bucket.s3.us-east-1.amazonaws.com/pimienta/sources/...
    FileAssetSearchRequest:
      type: object
      description: Filters and pagination for GET /api/v1/files/management.
      properties:
        page:
          type: integer
          format: int32
          default: 0
          description: Índice de página basado en cero.
          example: 0
          minimum: 0
        size:
          type: integer
          format: int32
          default: 20
          description: Tamaño de página (máximo 100).
          example: 20
          maximum: 100
          minimum: 1
        category:
          type: string
          description: Filter by category.
          enum:
          - TEMPLATE
          - COMPANY
          - EXTRAS
          - RESOURCE
          example: TEMPLATE
        module:
          type: string
          description: Filter by owning module (RESOURCE category).
          example: inventory
        entityType:
          type: string
          description: Filter by entity type.
          example: inventory-item
        entityId:
          type: integer
          format: int64
          description: Filter by entity id.
          example: 42
        originalNameContains:
          type: string
          description: Original filename contains (case-insensitive).
          example: invoice
        contentTypeContains:
          type: string
          description: Content type contains.
          example: spreadsheet
        uploadedByUserId:
          type: integer
          format: int64
          description: Filter by uploader user id.
          example: 1
        createdFrom:
          type: string
          format: date-time
          description: "Created from (inclusive), ISO-8601."
          example: 2026-05-01T00:00:00
        createdTo:
          type: string
          format: date-time
          description: "Created to (inclusive), ISO-8601."
          example: 2026-05-31T23:59:59
    DepartmentHeadcountResponse:
      type: object
      properties:
        department:
          type: string
        headcount:
          type: integer
          format: int64
    EmployeeSummaryResponse:
      type: object
      properties:
        totalNotDeleted:
          type: integer
          format: int64
        headcountByDepartment:
          type: array
          items:
            $ref: "#/components/schemas/DepartmentHeadcountResponse"
    EmployeeStatisticsResponse:
      type: object
      properties:
        total:
          type: integer
          format: int64
        active:
          type: integer
          format: int64
        notActive:
          type: integer
          format: int64
  securitySchemes:
    bearer-jwt:
      type: http
      description: "Send `Authorization: Bearer <access_token>`. Obtain tokens via\
        \ `POST /api/v1/auth/login` or `POST /api/v1/auth/refresh`."
      scheme: bearer
      bearerFormat: JWT
