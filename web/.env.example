# Browser-reachable API base (not the Docker service hostname).
# Must match the host port of the API (backend API_PORT, default 8080).
API_BASE_URL=http://localhost:8080/api/v1

# Comma-separated hosts allowed by Angular SSR (Host header check).
ALLOWED_HOSTS=localhost

# Host port mapped to the container SSR server.
PORT=4000
