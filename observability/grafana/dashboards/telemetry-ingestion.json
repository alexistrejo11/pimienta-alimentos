{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Telemetry received",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(pimienta_telemetry_events_received_total[5m])) by (source)",
          "legendFormat": "{{source}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Telemetry rejected",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(pimienta_telemetry_events_rejected_total[5m])) by (source, status)",
          "legendFormat": "{{source}} {{status}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "title": "POS health snapshots received",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(pimienta_telemetry_health_received_total[5m])) by (state)",
          "legendFormat": "{{state}}",
          "refId": "A"
        }
      ]
    }
  ],
  "schemaVersion": 41,
  "tags": ["pimienta", "telemetry"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Pimienta Telemetry Ingestion",
  "uid": "pimienta-telemetry",
  "version": 1
}
