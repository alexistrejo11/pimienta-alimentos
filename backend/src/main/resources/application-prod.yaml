# Profile: prod — Flyway migrations, schema validate, real AWS S3, tight actuator.
# Actuator shares server.port (API_PORT). Do not set management.server.port to the
# same value — that starts a second connector and breaks /actuator on the public port.
spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
    metrics:
      enabled: false
    prometheus:
      enabled: true
  info:
    java:
      enabled: false

pimienta:
  cors:
    allowed-origins: ${PIMIENTA_CORS_ALLOWED_ORIGINS}

logging:
  level:
    org.springframework.web: INFO
